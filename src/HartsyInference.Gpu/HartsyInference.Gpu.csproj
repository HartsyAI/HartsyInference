<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>HartsyInference.Gpu</PackageId>
    <Description>Shared GPU backend layer for HartsyInference: device-residency caching, op profiling and the backend base every GPU backend derives from. Vendor-neutral — contains no CUDA, Vulkan, ROCm or Metal interop.</Description>
    <RootNamespace>HartsyInference.Gpu</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HartsyInference.Core\HartsyInference.Core.csproj" />
  </ItemGroup>

</Project>
