<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>SharpInference.Cuda</PackageId>
    <Description>CUDA GPU inference backend for SharpInference with PTX kernels and cuBLAS integration.</Description>
    <RootNamespace>SharpInference.Cuda</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SharpInference.Core\SharpInference.Core.csproj" />
    <ProjectReference Include="..\SharpInference.Cpu\SharpInference.Cpu.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SharpInference.Cuda.Tests" />
  </ItemGroup>

  <!-- PTX kernel files, loaded from disk at runtime (CudaBackend: BaseDirectory/Ptx). -->
  <ItemGroup>
    <!-- One item, two jobs: copy PTX next to THIS project's own output (local build/tests),
         AND pack it into the nupkg under ptx/. SharpInference.Cuda.targets then copies it
         into a *consumer's* output at build time. -->
    <Content Include="Ptx\*.ptx" CopyToOutputDirectory="PreserveNewest" Pack="true" PackagePath="ptx/" />
    <!-- Ship the copy logic. build/ = direct package refs; buildTransitive/ = via meta-package. -->
    <None Include="SharpInference.Cuda.targets" Pack="true" PackagePath="build/;buildTransitive/" Visible="false" />
  </ItemGroup>

</Project>
