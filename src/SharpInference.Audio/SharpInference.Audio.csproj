<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>SharpInference.Audio</PackageId>
    <Description>Audio inference for SharpInference — Whisper STT, Kokoro TTS, voice conversion, mel-spectrogram preprocessing, audio codecs, and HuggingFace model caching.</Description>
    <RootNamespace>SharpInference.Audio</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SharpInference.Core\SharpInference.Core.csproj" />
    <ProjectReference Include="..\SharpInference.ModelHandler\SharpInference.ModelHandler.csproj" />
    <ProjectReference Include="..\SharpInference.Tokenizers\SharpInference.Tokenizers.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Test project needs access to internals (LstmCell, WeightNormFusion, codec sub-classes)
         so per-layer numerical correctness can be tested without lifting everything to public. -->
    <InternalsVisibleTo Include="SharpInference.Audio.Tests" />
  </ItemGroup>

</Project>
