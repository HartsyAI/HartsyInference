<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>HartsyInference.Video</PackageId>
    <Description>Video generation for HartsyInference — LTX-Video, Wan, temporal attention, and video VAE. (Phase 3 — stub)</Description>
    <RootNamespace>HartsyInference.Video</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HartsyInference.Diffusion\HartsyInference.Diffusion.csproj" />
    <!-- Cosmos-Predict1 V2W reuses the LLM AR primitives: FixedKvCache / IKvCache and the SamplerChain
         (temperature / top-p). LLM depends only on Core / ModelHandler / Tokenizers, so no cycle (Audio → LLM
         is the same established precedent). -->
    <ProjectReference Include="..\HartsyInference.LLM\HartsyInference.LLM.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="HartsyInference.Video.Tests" />
  </ItemGroup>

</Project>
