using Xunit;

// The parity theories in Parity/ open real GPU devices. Two GPU workloads on one card produce contention failures
// that look exactly like real ones — roughly twenty phantom failures the last time a suite ran them concurrently.
[assembly: CollectionBehavior(DisableTestParallelization = true)]
