# Wan-Animate-2 investigation

The length-dependent-defect hypothesis was retracted: base/distill sampling settings were mismatched.
Evidence and remaining work now live in [video status](MODEL_STATUS_VIDEO.md#wan-animate-2).
This redirect is retained for existing test-source references; the diagnostic journal is in git history.
