# Image Models — status

Concise status for every image-generation (diffusion / DiT T2I) model. Open work is in the
[Remaining work](#remaining-work) section below; bring-up debugging notes live in
[TROUBLESHOOTING.md](TROUBLESHOOTING.md). Parity evidence lives in
[PARITY_VERIFICATION.md](PARITY_VERIFICATION.md). Legend is defined in [MODEL_STATUS.md](MODEL_STATUS.md).

## Current performance vs ComfyUI

Canonical scoreboard (all models, both GPUs, dates and sources): **[`benchmarks/scoreboards/IMAGE.md`](../../benchmarks/scoreboards/IMAGE.md)**.

## Step-cache opt-in (2026-07-23, H1.5 — RTX 4090)

Additive to the baselines above — **not** a replacement, and the ComfyUI comparison table is
unaffected (default is OFF; this is an internal accelerator, opt-in only). Per-model calibrated
First-Block-Cache profile behind `HARTSY_STEP_CACHE=1` — the knob resolves each model's own
calibrated threshold/gate (raw / polynomial-rescaled / late-window, picked per model from its
drift-signature calibration; mechanics in
[ROADMAP.md §H1](ROADMAP.md)). SSIM is vs the uncached baseline image at
the shipped gate point; acceptance bar is SSIM ≥ 0.95 + eyeball. Swarm/production end-to-end
verification (via `/API/GenerateText2Image`, engine `2.0.0-alpha.2` (packed as alpha.64 during the pass, renumbered same-day)) covered
Krea2-Turbo, Ideogram 4, Flux.2 Dev, and Z-Image-Turbo (Qwen-Image was NOT included in that pass — its
numbers below are standalone-harness only); standalone warm A/B measurements are the primary numbers
below. The dated per-model write-ups these numbers were originally measured in have since been retired
(consolidated here and in the scoreboard above).

| Model | Baseline warm | Cached warm | Speedup | SSIM @ gate |
|---|---:|---:|---:|---:|
| Flux.2 Dev (50 st) | 97.5 s | **39.1 s** | **2.49×** | 0.958 |
| Ideogram 4 (20 st) | 19.5 s | **14.1 s** | **1.39×** | 0.953 |
| Qwen-Image (20 st) | 40.2 s | **33.0 s** | **1.20×** | 0.950 |
| Krea2-Turbo (8 st) | 4.43 s | **3.91 s** | 1.13× | 0.974 |
| Z-Image-Turbo (8 st) | 2.74 s | — **NEGATIVE** | — | — |

Z-Image-Turbo: no calibrated profile ships — residual-drift floor (0.44–0.51) is too high for any
quality-safe reuse at this model's 8-step schedule, so `HARTSY_STEP_CACHE=1` resolves to a harmless
0-reuse no-op.

Fleet standings at each model's shipped gate (all opt-in, all SSIM≥0.95 + eyeball-verified): **Flux.2
Dev 2.49× / Ideogram 4 1.39× / Qwen-Image 1.20× / Krea2-Turbo 1.13× / Z-Image-Turbo negative** — wins
scale with schedule length and drift-valley depth; short distilled schedules have little left to skip
(Wan is separately negative for per-seed identity — see `ROADMAP.md` §H1.5).

## Verified end-to-end (✅)

These produce clean visual output on real weights, confirmed end-to-end.

| Model | Status | Notes |
|---|---|---|
| **SD 1.5** | ✅ | Clean astronaut-on-horse output. **CLI catalog-path verified 2026-07-21**: `hartsy image -m sd15` (Comfy-Org/stable-diffusion-v1-5-archive fp16 single-file), `CliDrivable=true`. |
| **SDXL** | ✅ | Clean 1024×1024. Perf round 1 2026-07-09 (`alpha.44.24-local`): warm 1024²/20 steps **3.69s vs ComfyUI 3.7s (TIED)** — THE find was `Tensor.Reshape`/`.To()` on GPU activations forcing D2H sync + H2D re-upload (~457 multi-MB misses/step across the attention stack + skip clones); fixed by passing un-reshaped tensors to dim-explicit ops, allocating outputs in final shape, and device skip clones. ([details](#sdxl)) |
| **SD3.5 Medium** | ✅ | Clean photorealistic output; 5 pipeline bugs fixed (PHASE_3_DEVIATIONS #31-35). **CLI catalog-path verified 2026-07-21**: `hartsy image -m sd3` (Comfy-Org/stable-diffusion-3.5-fp8, all-in-one bundle), `CliDrivable=true`. |
| **Flux Dev / Schnell / Krea** | ✅ | Photoreal across all three. ([details](#flux-dev--schnell--krea)) |
| **Z-Image Turbo / Base** | ✅ | Clean photoreal; 8 plumbing bugs fixed (PHASE_3_DEVIATIONS #25-30). **CLI catalog-path verified 2026-07-21**: `hartsy image -m zimage` (mcmonkey/swarm-models FP8Mix repack), `CliDrivable=true`. |
| **Flux.2 Klein 4B** | ✅ | Clean astronaut. |
| **AuraFlow v0.3** | ✅ | Clean on-prompt horse+rider @1024 (`calcuis/aura` fp8). ([details](#auraflow-v03)) |
| **Qwen-Image** (20B MMDiT) | ✅ | Clean photoreal astronaut-on-horse @1024 (Q4_K GGUF + Qwen2.5-VL fp8 TE). ([details](#qwen-image)) |
| **Qwen-Image-Edit 2511** (20B, Q5 GGUF) | ✅ | Full edit conditioning e2e (`44.39-local`, 2026-07-10): suit-recolor + background-swap edits verified with subject identity preserved. ([details](#qwen-image-edit-2511)) |
| **Anima** (Cosmos-Predict2 2B) | ✅ | Clean on-prompt anime @512 on the 3060 (Qwen3-0.6B embeds). ([details](#anima)) |
| **Lumina-Image 2.0** (2B NextDiT) | ✅ | Clean on-prompt mountain-lake @512 (53s). ([details](#lumina-image-20)) |
| **Chroma** (8.9B fp8) | ✅ | Clean on-prompt astronaut-on-horse @512 (painterly/ink style). ([details](#chroma)) |
| **Krea 2 Turbo** (12.9B MMDiT, fp8) | ✅ | Sharp photoreal astronaut-on-horse @1024, 8-step (std 66.5, grid 0.042). ([details](#krea-2-turbo)) |
| **Kandinsky 5.0 Lite** (BF16) | ✅ | Sharp on-prompt snow-leopard-on-peak @512 (std 90, grid 0.084), 64s on 4090. 3 fixes: OOM (12GB BF16 cast to F32 → cast to **F16** instead, fits 24GB), then BLANK (F16 fixed weights-not-applied), then NOISE = **`CudaBackend.LayerNorm` F32-input path didn't cast F16/BF16 affine weights** → text_proj/pooled_proj collapsed to ~0 → conditioning dead (same dtype-mismatch class as GroupNormSilu; GENERAL fix). ([details](#kandinsky-50-lite)) |
| **Boogu-Image 0.1 Base** (10B, fp8) | ✅ | Sharp photoreal astronaut-on-horse @1024-cfg (std 97.8, grid 0.038), **~6 min** (under the 10-min bar). ([details](#boogu-image-01-base)) |
| **Boogu-Image 0.1 Turbo** (10B, fp8) | ✅ | Sharp photoreal astronaut-on-horse @1024, 4-step tg=1.0. Shares Base's config/TE/VAE/converter. ([details](#boogu-image-01-turbo)) |
| **ERNIE-Image** (8B fp8) | ✅ | Sharp full-contrast astronaut-on-horse @512-cfg (std 60.9, grid 0.069). ([details](#ernie-image)) |
| **HiDream i1 Dev** (17B fp8, quad-encoder + MoE) | ✅ | Sharp photoreal astronaut-on-horse @1024, verified full 25-step. ([details](#hidream-i1-dev)) |
| **OmniGen 2** (fp16, Qwen2.5-VL-conditioned MMDiT) | ✅ | Coherent astronaut-on-horse @512-nocfg AND @1024-CFG. ([details](#omnigen-2)) |

## Numerically verified, full e2e pending (🔬)

| Model | Status | Notes |
|---|---|---|
| **Ideogram 4** (9.3B DiT) | ✅ | e2e coherent + prompt-faithful (structured-JSON prompt), visually verified. ([details](#ideogram-4)) |

## Built, validation-pending (🔧)

All implemented end-to-end (pipeline + converter + tests), green and structurally tested; each awaits a
checkpoint download + a Python layer-diff pass (and several are gated on VRAM). See the Remaining work section for the
per-model architecture notes and build plans.

| Model | Notes |
|---|---|
| **Flux.2 Dev (32B)** | ✅ **FIRST E2E 07-10** (`44.48-local` + extension): sharp on-prompt astronaut @1024²/20st, warm **52.6s** (peak 23.3GB; step-graph capture OOMs → eager fallback; **ComfyUI baseline 54.37 s (07-18, via city96 GGUF node) → 0.97× ~tied/faster**, `image_python_baselines_2026-07-18.md`). ([details](#flux2-dev-32b)) |
| ~~ChromaRadiance~~ | ✅ **PERF PASS 07-18 (working-tree on `1.0.0-alpha.51`) — 21.46 s fp8; honest fp8-vs-fp8 = 1.02× MATCHED** (Comfy's best fp8 21.07 s; within run-noise). F32 31.18→F16 backbone 30.27→fp8 22.53→CopyInto-fix 22.37→**F16 NeRF head 21.46** (F32 BatchedMatMul hypernetwork+GLU→F16 tensor cores; param_generator un-damp GEMM+L2-normalize stay F32 for F16-overflow safety; coherent). peak VRAM 22→14 GB. Progression: the "2.20×/54.4 s" was STALE (already 31.18 s F32) → F16 backbone opt-in (deferred Radiance-head audit: img damped at ForwardCore F16 cast, un-damped at NeRF param_generator GEMM alpha — coherent+exact) only −1 s → **fp8 weight requant** (`HARTSY_RADIANCE_FP8`: `QuantizeDitBlocksToFp8` on block Linears in `ChromaCheckpointConverter`; a SYNC profile showed Linear=58 %, BF16-weight-HBM-bound → fp8 halves that traffic; native fp8 tensor-core GEMM consumes the F16 acts + absorbs the damp scale) → **22.53 s**. **Honest bench** (both fp8; Comfy = `--fp8_e4m3fn-unet --fast fp8_matrix_mult`): Comfy **21.07** vs ours **22.53** → gap closed 1.23×→1.07× but not beaten. (Comfy fp8 *without* fp8_matrix_mult = 26.37, i.e. slower than its own BF16 24.68.) Follow-ups to beat it: persist fp8 repack (load = ~76 s host absmax loops), + profiled H2D_MISS_BIG 1.3 s / CopyInto 0.67 s / fp8 the NeRF head. (`image_python_baselines_2026-07-18.md`). **warm 54.4s @1024²/20st/cfg3.5 (2.65 s/step)**. The cost was NOT the pixel-space token count (4096 img tokens = classic Chroma @1024²) — it was the NeRF head's host tile loop (~50k tiny Linears + host normalize/silu/scatter per forward, ×2 CFG). GPU port with existing ops: embed Linear folded into a stride-16 conv at load, hypernetwork GLU batched via BatchedMatMul (batch=patches, 1024-patch tiles to fit beside the 19 GB BF16 resident DiT), RmsNorm-as-L2-normalize, device UnpatchifyTokens; + ForwardPaired (shared modTable/patchify/embed per CFG pair), prompt cache, context trim (mask-free SDPA), DiT residency, drain-free ToVelocityDevice+CfgEulerStep loop. **Parity corr 0.99996** vs pre-port engine (4-step seed-42 A/B); 20-step astronaut VIEWED coherent. Dark/backlit + heavy vignette look matches this WIP checkpoint's known character (Swarm rates it "Bad Quality (WIP)") — pass with caveat. Next levers: F16 head/backbone (damp-kit audit), Radiance step graph, fp8 requant of the 19 GB BF16 file. **CLI catalog-path wiring 2026-07-21**: `hartsy image -m chroma-radiance` (`Comfy-Org/Chroma1-Radiance_Repackaged`, `chroma-radiance-x0.safetensors`, 19GB) reproducibly gets killed partway through the host-side `ChromaCheckpointConverter.LoadAndConvert` (observed growing to 25GB+ resident RAM before disappearing, 3 attempts, up to a 900s timeout) — this box has only 62GB RAM / 2GB swap, consistent with an OOM-kill rather than a hang (CPU pegged at 100%, GPU stayed at 0% the whole time, i.e. it never got past host-side load). Not the same checkpoint variant this doc's perf-pass rows were verified against (those used an fp8-requantized load path, `HARTSY_RADIANCE_FP8`, "~76s host absmax loops" — much cheaper than a full-precision load). Not resolved here (a real fix needs either a leaner host-side conversion path or more RAM than this box has); `CliDrivable` left `false`. |
| ~~ZetaChroma~~ | ✅ **VERIFIED CLEAN 07-11** (`44.70-local`, carried in `44.71`): coherent astronaut-on-horse, confetti gone, visually comparable to the ComfyUI baseline. The periodic tile artifact was **BF16 `dec_net` in_ln weight/bias read as host float*** in `ZetaChromaDecoderHead` (garbage per-channel affine in all 4 AdaLN res blocks → 32-px RGB confetti); root-caused via `HARTSY_ZETA_DUMP` stage dumps vs a torch replay (`dec_h_block0` relL2 1.24, BF16-reinterpret replay matched the engine at 3.5e-3). Fix = `EnsureF32HostReadable` at load. Sampling recipe (x0, uncond-anchored CFG, dynamic shift) was already correct. Same bug class as F-Lite `register_tokens` — audit host float* weight reads for BF16. Split-attention fuse (to_q/k/v→qkv) shipped `44.53`. |
| ~~F-Lite~~ | ✅ **CORRECT + BENCHED 07-11** (`44.67-local`): **Python baseline 122.98 s (07-18, diffusers `f_lite` sequential CPU offload — 10B+T5-XXL≈29 GB > 24 GB so resident/model-offload OOM; memory-bound not compute) → 0.50× faster** (`image_python_baselines_2026-07-18.md`). coherent reference-quality astronaut @1024²/30st/cfg6, **warm 61.5s** (prompt-cache hit skips T5; peak 23.8GB). Byte-oracle campaign vs fal-ai found 4 real bugs: register_tokens + lambda_param are **BF16 read as host float*** (bytes reinterpreted → garbage registers + broken V-residual), alpha from the LATENT grid (4 @1024², not 2), and reference T5 conditioning = NO attention mask + zeros-tensor negative. Plus the GPU-residency port (90→1.9s/step, 47×, host-glue AV gone) and TE⇄DiT staging + prompt cache. Old note: ⚠️ BLOCKED ON THE KNOWN HOST-GLUE CRASH 07-10: loader fully works (`f-lite` model class registered in the extension — diffusers-folder shards scan architecture-less otherwise), TE+DiT+VAE load, denoise runs @1024²/30st — but **90s/step, GPU 11%** (glue scan: FLiteBlock cpu=24, FLiteAttention cpu=13, FLiteRope cpu=19 HOST rope, FLiteTransformer cpu=40) and dies ~step 6 with the documented `cpu-glue-async-race` AccessViolation. Fix for BOTH stability and speed = the GPU-residency block port (perf-pass queue #1). **CLI catalog-path re-verified 2026-07-21**: 1024² reproducibly OOMs on this 24GB 4090 (even in total isolation, even inside the tiled-VAE-decode recovery path) — the "peak 23.8GB" figure above leaves only ~700MB headroom on a 24564MB card, which real allocator/context overhead now eats into; this is a genuine ragged-edge memory ceiling, not a regression (same class as zeta-chroma's 1024 ceiling). **768×768 succeeds cleanly**: clear on-prompt astronaut in full white spacesuit/helmet on a brown horse, no artifacts. `CliDrivable=true` with 768 as the practical default until this gets a real memory-budget pass. |
| **Hunyuan Image 2.1** | ✅ **FIRST E2E 07-10** (`44.52-local`): sharp 2048²/20st/cfg3.5 astronaut, warm **~50.0s** (**ComfyUI baseline 48.08 s → 1.04× MATCHED**; 07-18 A/B on `alpha.61`: F16 50.1 s ≈ F32 49.9 s. ([details](#hunyuan-image-21)) |
| **F-Lite (Freepik / Fal.ai)** | T5-XXL layer-17 encode; ~29.4 GB checkpoint. |

## Edit / image-conditioned variants (🔧 — to download + e2e test)

Instruction/edit models reuse the base transformer with image-conditioning slots. These need their own e2e tests + weights:

| Model | Notes |
|---|---|
| **Boogu-Image 0.1 Edit** | ✅ **07-16 e2e (engine main + extension fix)**: instruction edit verified on the real fp8_scaled checkpoint (apple → yellow pear, scene identity preserved; 768², 28 steps, tg=4/ig=1, seed-exact repro of the live failure then the fix). `InstructPix2PixCfg` → `imageGuidanceScale` re-wired natively 2026-08-07 (`BooguImageRecipePipeline`, default 1.0, drop-all embedding supplied when ig>1). ([details](#boogu-image-01-edit)) |
| **Flux.1 Kontext** | ✅ **07-16 e2e (`49.6-local`)**: clean instruction edit (apple recolor, composition/identity preserved). ([details](#flux1-kontext)) |
| **Flux.1 Fill** | ✅ **07-16 e2e (`49.1-local`)**: engine 384-ch conditioning implemented ([noise 64 \| masked latent 64 \| packed mask 256], diffusers-parity pack order unit-tested), FluxLoader detects 384-wide x_embedder + requires Init+Mask (strength defaults 1.0). Swarm verify: masked apple → photoreal strawberry bowl, surroundings preserved. Mask-edge seam fixed by binarizing the pixel-zeroing mask (Comfy parity); re-verify after next deploy. |
| **Flux.1 Canny** | ✅ **07-16 e2e (`49.1-local`)**: full pipeline (edge map → VAE → 128-ch concat) verified — marble apple following the reference contour exactly (guidance 30, BFL-recommended). ([details](#flux1-canny)) |
| **Flux.1 Depth** | ✅ **07-16 e2e (`49.4-local`)**: depth-faithful gens (synthetic-map ball test + cropped-reference apple, both clean). ([details](#flux1-depth)) |
| **Flux.1 Redux** | ✅ **07-16 e2e (`49.1-local`)**, regressed by the DTO refactor (`a688dc4` deleted the extension `ReduxResolver` call site; the engine resolver was ported but nothing called it), **restored 2026-08-07 engine-side**: `Flux1RecipePipeline` now calls `Features/ReduxResolver.ResolveAsync` from the `redux.stylemodel`/`redux.multiply`/`redux.merge` (number 0..1, was mis-doc'd as string)/`redux.apply_start` Extra keys + `IpAdapter.PromptImages`; extension maps Comfy's `usestylemodel` + strength params onto those keys. Resolver math still parity-valid (07-17 corr 1.000000); the new call-site path needs one fresh e2e run. ([details](#flux1-redux)) |
| **Microsoft Lens / Lens-Turbo / Lens-Base** | 3.8B dual-stream MMDiT + GPT-OSS MoE encoder + Flux.2 VAE. ✅ **07-16 e2e VERIFIED on the 3060**: photoreal on-prompt corgi-in-top-hat @1024², turbo 8 steps cfg=1 seed=42 (`lens_turbo_gpu_1024x1024_s8_seed42.bmp`, neighbor-corr 0.993; TE encode 84.5s + ~25s/step host-glue-bound — perf grind is the follow-up). **Correctness reconciliation vs the ComfyUI reference** (`comfy/ldm/lens/` + `comfy/text_encoders/gpt_oss.py`): DiT real-weight layer parity vs `lens_turbo_bf16` — all 48 blocks + final layers corr ≥0.999 (sampled stages corr 1.0000000), `dump_lens_reference.py` (the C# layer-diff test was removed 2026-08-06). 4 bugs fixed: (1) `norm_out` AdaLN-Continuous chunks **[scale, shift]** (scale FIRST — opposite of Flux/SD3/Qwen; engine had [shift, scale] → uniform noise); (2) chat template was NOT the Harmony render (missing "You are ChatGPT…" system preamble + developer block + `<|channel|>final`) while still stripping txt_offset=97 → conditioning was garbage; (3) `GptOssTokenizer` couldn't emit the o200k_harmony specials (exported vocab lacks added tokens) + no o200k split regex/byte-level remap + ML.Tokenizers' default pre-tokenizer breaks punctuation merges — rewritten, byte-exact vs HF `tokenizers` oracle incl. the full template (wrapper = exactly 97); (4) prompts were right-padded to 512 with `<|return|>` and fed to the DiT unmasked — now true-length, cond/uncond as separate forwards (mathematically identical to the reference's pad+mask batch-of-2). TE architecture (YARN rope θ=150000/factor 32, sinks, alternating sliding-128/full, MoE top-4-then-softmax, interleaved gate/up clamp-7 GLU) verified line-by-line vs the reference; memory-bounded NVFP4 path intact (`LensTeMemoryBoundedLoadTests` green). **CLI catalog-path verified 2026-07-21**: `hartsy image -m lens` (`Comfy-Org/Lens`, `diffusion_models/lens_turbo_bf16.safetensors`), sharp on-prompt astronaut-on-horse @1024, turbo 4-step. `CliDrivable=true`. ⚠️ **REGRESSION FOUND 2026-07-26**: the SwarmUI `/API/GenerateText2Image` path (not the CLI path verified above) produces **solid-black output, 16/16 times** across both variants (`lens_turbo_bf16`, `lens_bf16`) and 2 independent param sets (official defaults incl. turbo 4-step/cfg1 AND an off-default 8-step/cfg1) — no error or warning logged, generation "completes" successfully with a black PNG. ComfyUI produces correct, sharp, on-prompt output from the **identical checkpoint files** via the same SwarmUI routing, 0/16 failures — ruling out a bad checkpoint. Either this is a regression since 07-21 or the API path never exercised the same code as the CLI verification. See `docs/Checklists/TROUBLESHOOTING.md` (P0 #1) for full repro. ✅ **RESOLVED 2026-07-27** (PHASE_3_DEVIATIONS #48): not an API-vs-CLI divergence — the CLI reproduced the black output identically, so the 07-21 CLI verification simply predates the trigger. SageAttention's INT8 flash path (default-on for no-mask F32 MHA at `head_dim ∈ {64,128}`) casts V to F16 in its `LaunchSageVF16T` prologue, and Lens is the one arch whose V is un-normalized (Q/K are RMS-normed, V is not) with a residual stream hot enough — reference-confirmed, block-2 image max_abs 3.09e7 ours vs 3.16e7 ComfyUI — that `max|V|` crosses F16's 65504 from the third denoise step on (18940 → 31281 → 71583). One INF V column NaNs the entire joint sequence through `softmax·V`, and NaN maps to byte 0 in `TensorToRgbBytes`. `allowF16: false` was already passed and is silently ignored by both Sage branches. Fixed in `LensTransformerBlock` by scaling V by 1/256 into the SDPA and back out of its output (exact — attention is linear in V; peak `max|V|` measured at 139264 over the 4/20/50-step schedules). Re-verified 2026-07-27: `lens_turbo_bf16` @ 4 steps/cfg 1 and @ 20 steps/cfg 5, plus `lens_bf16` @ 20 steps/cfg 5 — all sharp and on-prompt, `quality_gate.py` 4/4 unflagged. |
| **Lance (ByteDance) image** | Unified multimodal 3B-active (MoT dual-stream); shares backbone with Lance video. **07-16 real-checkpoint reconciliation + backbone parity**: first released `Lance_3B` (11.8GB BF16) exposed 4 port bugs — guessed head keys (`vae_in/vae_out` → real `vae2llm [2048,48]`/`llm2vae`, latent patch is **(1,1,1)** not (1,2,2), 16× total downscale), QK-norm ships ON (`q_norm/k_norm [128]` + `_moe_gen`), `max_latent_size=64` with the frozen `latent_pos_embed [4096,2048]` table loaded from the ckpt (not recomputed), and **MaPE does NOT apply to T2I** (only `full_noise`/`full` edit splits) — positions are plain Qwen2.5-VL `get_rope_index` over the ChatML template (`< | video_pad | >` slots even for images; noise split includes the vision sentinels).` ([details](#lance-bytedance-image)) |

## Conditioning adapters (ControlNet / IP-Adapter) — ✅ 07-16 (`49.7-local`)

| Feature | Status |
|---|---|
| SDXL ControlNet | ✅ canny (strong adherence), depth mode, 2-net stacking, start/end step window. LDM (`control_model.*`) + diffusers layouts both load (new `ControlNetCheckpointConverter`, 340/340 key parity vs the diffusers pair). `diffusers_xl_depth_full` is weak — prefer `xinsir/controlnet-depth-sdxl`. |
| SDXL **Union** ControlNet (xinsir controlnet-union-sdxl ProMax) | ✅ LIVE-VERIFIED 07-17 via SwarmUI (canny + depth types, contour/depth-faithful; plain-CN regression clean). Engine union path landed 07-16: `ControlNetUnionFusion` (task_embedding row add + `transformer_layes` fusion transformer + zero-init `spatial_ch_projs` per-channel bias + `control_add_embedding` multi-hot control-type sinusoid MLP into temb), loader auto-detect via `task_embedding`/`control_add_embedding` keys → `ControlNetConfig.UnionControlTypeCount`, type ids `SdxlUnionControlType` (0 openpose, 1 depth, 2 softedge, 3 canny/lineart, 4 normal, 5 segment, 6 tile / 7 repaint = ProMax-only), plumbed via `ControlNetConditioning.UnionControlType` through `ForwardStacked` (stack the same union adapter with different images/types = diffusers MultiControlNetUnion). **Parity vs diffusers `ControlNetUnionModel` on the real ProMax checkpoint: all 9 down + mid corr ≥0.9999998, maxAbs ≤5.4e-4 relative to each residual's peak** (canny B=1 + depth B=2; `dump_sdxl_controlnet_union.py`; the C# test was removed 2026-08-06). Checkpoint: `Models/controlnet/controlnet-union-sdxl-promax.safetensors` (2.5GB F16, 8-type ProMax — drop-in superset of the 6-type standard union). Extension wiring deployed 07-17 (union-type dropdown routed to `SdxlUnionControlType`). |
| SD1.5 ControlNet | ✅ pipeline wiring landed 07-16; canny + depth verified e2e (watercolor apple / vase). |
| Flux DiT ControlNet (union + single-mode) | ✅ LIVE-VERIFIED 07-17 via SwarmUI on the real 4.3GB Union-Pro-2.0 checkpoint (`flux-union-pro2-instantx`, flux1-dev-fp8 base, strength 0.8): contour-locked glass-apple sculpture following the reference shape/position (also verified 07-16 evening). Engine adapter landed 07-16: `FluxControlNet` (diffusers `FluxControlNetModel` port — reduced double/single block stacks reusing the Flux blocks, zero-Linear per-block residuals, `controlnet_x_embedder` over the packed VAE latent, union `controlnet_mode_embedder` prepend), `FluxTransformer` interval-mapped additive injection (F16-damp-compensated, host path), `FluxPipeline` `fluxControlNets` param (multi-CN stacking + start/end windows), loader auto-detect (`ControlNetFile.FluxConfig` — depths/union/guidance from the header; Union-Pro-2.0 = 6 double + 0 single, NO mode embedder — verified vs the real checkpoint). **Parity vs diffusers `FluxControlNetModel` maxAbs 3.7e-9** (union 2+2 w/ mode token AND plain 2+0 variants, tiny synthetic weights, CPU; `dump_flux_controlnet.py` + `FluxControlNetParityTests`). Extension wiring deployed (`FluxControlNetResolver`). |
| IP-Adapter SDXL std/Plus + SD1.5 std | ✅ verified e2e (subject/style transfer; Plus = near-identity). THE black-output root cause: checkpoints store the per-layer K/V list in diffusers **enumeration order (down → up → mid LAST)**, engine consumed traversal order → wrong weights/NaN. Also fixed: Plus misdetection via `image_proj.norm.weight` (standard files have it too), SDXL Plus resampler dims (1280/20-heads, not 1024/12), IPA cache entries evicting the base pipeline at MaxCachedPipelines=1. **FaceID** (ArcFace IR-50, cosine 1.000000) live-verified 07-16/17. **FaceID-Plus/PlusV2** (SD1.5+SDXL): projection parity corr 1.000000 vs torch on all 3 real checkpoints + SDXL plusv2 e2e identity cosine 0.3456, live-verified 07-17 (see PARITY_VERIFICATION). Standard SDXL/SD1.5 + Plus + plus-face all re-verified live 07-17 after the shared ResamplerLayer GELU tanh→erf parity fix. |
| Preprocessors | Canny (fixed: no pre-blur + L1 magnitude — old maps were near-empty), Depth-Anything-V2 (in-engine, parity 2.9e-7), OpenPose (engine `OpenPosePreprocessor`: YOLO11-pose reuse + BODY-18 render; SD15 openpose CN verified e2e), HED/softedge + scribble + Lineart + NormalBAE (in-engine annotators, parity ≤8e-6; extension wrappers in ext commit 40545d1), **Segmentation** (in-engine `UperNetSegPreprocessor`, UperNet-ConvNeXt-Small ADE20K, class agreement 100% vs transformers + byte-exact `ade_palette()`; `control_v11p_sd15_seg_fp16` load-verified through the LDM converter; extension `segment` wiring deployed + LIVE-VERIFIED 07-17: watercolor street follows the bus photo segment layout). Raw-map passthrough pending. |

## How to promote a 🔧 to ✅

Download the checkpoint, point the test paths at it, run the model's generation test, then iterate with
its `*DebugDump` hooks against the Python `dump_*_full_forward.py` + `diff_*_layers.py` harness until the
first layer with `avg_err > 1e-3` is fixed. Step-by-step unblock recipes per model are in the
[Remaining work](#remaining-work) section below.

## Remaining work

Distilled from the retired PHASE_4_MODEL_BREADTH / PHASE_3_DEVIATIONS plans. Models already ✅ above
(Ideogram 4, Lens, ERNIE-Image, Chroma, AuraFlow, Hunyuan Image 2.1, Flux.2 Dev 32B, Lance) are omitted.
See [ROADMAP.md](ROADMAP.md) for cross-cutting infra (multi-GPU, kernel perf, quant, serving).

### Numeric validation
- [ ] Run a first real-checkpoint layer-diff for each 🔧 built-but-validation-pending model to promote it to verified-e2e.

### Not yet built (more-model support)
- [ ] Krea 2 non-turbo base/CFG path (researched, not built; Turbo is ✅).
- [ ] Flux.2 Klein 9B (no public weights).
- [ ] F-Lite Freepik / Fal.ai variant (T5-XXL layer-17, ~29.4 GB checkpoint).

### Pipeline / plumbing
- [ ] Quality-preset per-pipeline constructor wiring.
- [x] **Regional/bbox-prompting plumbing on Flux.2 and Krea2 — DONE 2026-08-11 (Tier 3.7).** Both
  `Flux2Transformer.Forward`/`ForwardWithTemb` and `Krea2Transformer.Forward`/`ForwardPatched`/`ForwardCore` now
  accept an `attnBias` tensor threaded down to each block's `ScaledDotProductAttention` call, mirroring
  `FluxTransformer.Forward`'s existing slot. `Flux2Pipeline`/`Krea2Pipeline.GenerateFromTokens` gained a
  `regionalPlan` param + an `EncodeRegionText` method (Flux.2: same multi-layer text-encoder taps the base
  prompt uses; Krea2: wraps the existing `EncodeTapped`), and `Flux2RecipePipeline`/`Krea2RecipePipeline` gained
  a `BuildRegionalPlan` mirroring `Flux1RecipePipeline`'s. Both recipes now declare `ImageFeatures.Regional`.
  Krea2-specific risks (GQA head-repeat-before-SDPA, token concat order) were confirmed safe by reading the code
  before wiring, not assumed by analogy — real-weight verified on both architectures with a two-region
  red/blue prompt (clean per-half color split, visually confirmed on Flux.2's local GGUF Dev build and Krea2's
  local fp8 checkpoint). CUDA-graph capture and Flux.2/Krea2's own DiT-sharding routes are excluded when a
  regional plan is active (mirroring `FluxTransformer`'s cache/graph exclusions) — Krea2's sharded route has no
  bias parameter at all, so this is a real correctness requirement, not just a perf trade-off; a warning logs
  once per generation when sharding was configured but overridden, rather than silently dropping the
  conditioning.
- [ ] **Recipe/pipeline embedding-delegate boundary** (blocks regional/bbox prompting on Flux.1, Z-Image,
  Ideogram 4 even though all three pipelines already have the attention-bias hook and
  `RegionalPromptResolver.Resolve` exists and is tested) — recipes own the tokenizer, pipelines own the text
  encoder and compute the base-prompt conditioning tensor internally, and `Resolve` needs both. No current
  recipe/pipeline API shares a base-prompt embedding tensor or an arbitrary-text-encode delegate across that
  boundary. Needs a design decision on the restructure before this is wiring work rather than architecture work.

### SDXL — open items found during the tiled-VAE-encoder / textual-inversion work (2026-08-09)
- [ ] **F16 VAE "black output" bug also affects SDXL**, not just Flux Schnell as previously documented —
  confirmed via A/B (identical failure with and without an unrelated in-flight code change) at 1536x1536
  with the checkpoint's native F16 VAE weights; forcing F32 VAE weights sidesteps it. Root cause not yet
  investigated for the SDXL case specifically.
- [ ] **ClipSkip is ignored whenever SDXL's dual-CLIP conditioning goes through a `conditioningSchedule`
  override** (weighted-prompt `(word:1.2)` syntax or a textual-inversion embed both build one) —
  `SdxlRecipePipeline` hardcodes `SdxlLayersFromEnd` (2) on that path regardless of `request.ClipSkip`; the
  plain no-override path does honor it correctly. Only visible when a request combines a non-default
  ClipSkip with either feature.

### Deferred deviation follow-ups
- [ ] Gemma-2 attention logit soft-capping @50 + alternating local/global sliding-window + per-query sqrt(head_dim) scalar.
- [ ] Conv2D BF16 branch (if a future model needs it).
- [ ] Migrate `ExtractPooledOutput` to `backend.Linear`.

## Details

Verification evidence, bugs found, and caveats for the rows above. Moved out of the status
tables on 2026-08-06 so the tables stay scannable — no content was dropped.

### SDXL

Clean 1024×1024. Perf round 1 2026-07-09 (`alpha.44.24-local`): warm 1024²/20 steps **3.69s vs ComfyUI 3.7s (TIED)** — THE find was `Tensor.Reshape`/`.To()` on GPU activations forcing D2H sync + H2D re-upload (~457 multi-MB misses/step across the attention stack + skip clones); fixed by passing un-reshaped tensors to dim-explicit ops, allocating outputs in final shape, and device skip clones. Plus batched-CFG single forward, drain-free `CfgEulerStep` loop (Euler/epsilon only), cached ADM embedding, dual-CLIP prompt cache, KEEP_MODELS. Seed A/B corr 0.9990. SD1.5/Refiner/Inpaint inherit the block fixes. Round 2 (`44.26-local`): **2.93s (1.26× faster than Comfy)** — cuDNN conv forward (new `CudnnConv`, default-on, fleet-wide), cuBLASLt bias-epilogue promoted to standard profile, VaeAttention Reshape purge (VAE 377→235ms), cross-attn K/V cache. GPU-bound at 100% util ≈63% of F16 peak; **sub-2s needs fp8 UNet GEMMs (output-changing — a quality decision) — see the benchmark log round-3 menu**. Remaining F16 levers: GroupNormSilu grid (~−0.15s), fused QKV (~−0.1s), cold TE 4.4s (ClipTextEncoder Reshape audit). **Multi-GPU (2026-08-05)**: TE + VAE placement (`PlacementConfig.TextEncoderDevice`/`VaeDevice`, extension `TextEncoderGpuId`/`VaeGpuId`, CLI `--te-gpu`/`--vae-gpu`) verified on the 4090+3060 box — `SdxlComponentPlacementEngineTests`, SSIM 0.9998 vs single-GPU.

### Flux Dev / Schnell / Krea

Photoreal across all three. **CLI catalog-path verified 2026-07-21** for Dev (`hartsy image -m flux1`, Comfy-Org/flux1-dev fp8) and Krea Turbo (`hartsy image -m krea2`); both `CliDrivable=true`. **Multi-GPU (2026-08-05)**: TE + VAE placement verified (`FluxComponentPlacementEngineTests`, SSIM 0.8126 — the fp8 T5 drifts across dissimilar SMs on the mixed 4090+3060 pair; matched cards are expected bit-identical); DiT block-range sharding (`DitShardGpuId` / `--dit-shard-gpu`, VRAM pooling not latency) verified on the plain path — `FluxDitShardingVramTests` (same-device split BIT-EXACT over 262k values; cross-device 30/57 byte-weighted split pools 7.7+3.7 GB) + `FluxDitShardingEngineTests` (e2e SSIM 0.9075); ControlNet/Kontext/inpaint/regional fall back to unsharded with a log; CFG-parallel fallback observability green (`FluxCfgParallelFallbackTests`; `Flux1Recipe`'s missing `CfgParallelBackend` forwarding fixed 2026-08-04).

### AuraFlow v0.3

Clean on-prompt horse+rider @1024 (`calcuis/aura` fp8). Two fixes: Pile-T5-XL attn scale 1.0 + correct `pile_t5xl_spiece.model` tokenizer. See PARITY §Bugs. **CLI catalog-path bug found + fixed 2026-07-21**: `AuraFlowRecipe.Construct` had regressed to constructing `T5Tokenizer` with the embedded Google-T5 vocab (the TODO(E-IMG-5) "correct tokenizer" fix from the note above was never actually landed in the shipped recipe, only proven in the AuraFlow generation test, since removed) — produced coherent-but-garbled-text, off-prompt output. Fixed by resolving `SideModels.PileT5XlSpiece` (new entry, `EleutherAI/pile-t5-xl/spiece.model`) and passing its path to `T5Tokenizer`. `hartsy image -m auraflow` now verified end-to-end (clean painterly scene, no garbled text); `CliDrivable=true`. **Prompt-adherence follow-up (re-verified after initial under-testing)**: at the catalog default cfg 3.5 with no negative prompt, "an astronaut riding a horse" rendered only the horse; a plain-text retry at cfg 5.0 + a negative prompt rendered only the astronaut (no horse). Neither is a wiring/conditioning bug — a third attempt with more explicit compositional phrasing ("a photo of an astronaut sitting on top of a brown horse in a green field, the astronaut is wearing a white spacesuit and helmet", `-n "text, watermark, poster, collage, painting, blurry, cropped"`, cfg 6.5, seed 7) rendered **both subjects together correctly**. AuraFlow needs explicit compositional language + a style-negative prompt + cfg≈6-7 to reliably bind two subjects in one scene; terse prompts at the family-default cfg 3.5 are not sufficient for compound scenes. `CliDrivable=true` confirmed genuine (not just crash-free).

### Qwen-Image

Clean photoreal astronaut-on-horse @1024 (Q4_K GGUF + Qwen2.5-VL fp8 TE). 4 bugs fixed (final-layer scale/shift, conditioning template+drop, GGUF shape relabel, weight-cast OOM) + GPU-residency perf rewrite. See PARITY §Bugs. Perf grind 2026-07-08 (`alpha.44.8-local`): warm 1024²/20 steps **40.9s vs ComfyUI 54.8s (1.34× FASTER)** — device joint RoPE (was per-block host loop), cuDNN flash SDPA, fused device CFG+Euler drain-free loop, TE prompt cache, KEEP_MODELS. Next lever (documented in the benchmark doc): Q4_K→fp8 requant at load (est. →~30s). **CLI catalog-path verified 2026-07-21**: `hartsy image -m qwen-image` (`QuantStack/Qwen-Image-GGUF` Q4_K_M), sharp on-prompt astronaut-on-horse @1024/20st. **Low-VRAM 2026-07-27**: `QwenImageBlock` now implements `IStreamingBlock` and `QwenImageTransformer` exposes `BlockCount`/`GetBlock`/`BeforeBlockForward` + a shared-vs-block split, so the denoise loop streams its 60 blocks when the resident layout will not fit. Previously this pipeline used the streaming cache **only** as a VAE-decode eviction gate — the loop itself was all-or-nothing, which is why a 12 GB card OOM'd after uploading 1,675 weights. **Verified running on a 12 GB RTX 3060**: 1024²/20 steps in 231 s, peak 9,959 MiB, quality-gate clean and visually sharp (14.3 GB needed vs 9.7 GB available, so it does not fit resident). Note the shared weights *bracket* the blocks in `EnumerateWeights` (input projections before, output head after) — `EnumerateSharedWeights` is a genuine partition, not a prefix. Host-side load/convert of the 20B checkpoint was slow enough on this box (16GB text_encoders + 13GB GGUF, shared 62GB RAM) that a 500s timeout wasn't enough on the first clean attempt — needed ~900s; not investigated further (matches the general pattern of large-checkpoint host conversion being RAM/CPU-bound here, same class as the ChromaRadiance and Anima-@1024 findings). `CliDrivable=true`. **Multi-GPU (2026-08-05)**: DiT block-range sharding verified on real weights — `QwenImageDitSharding{,Vram,Engine}Tests`: 19.6 GB pooled 13.4+6.2 across 4090+3060 at the live 41/60 split, e2e SSIM 0.9734 vs unsharded, cross-generation VRAM drift 0.00 GB; TE/VAE placement wired (2026-08-04 wave, incl. the edit-reference encode) but awaiting a checkpoint re-download for engine verification.

### Qwen-Image-Edit 2511

Full edit conditioning e2e (`44.39-local`, 2026-07-10): suit-recolor + background-swap edits verified with subject identity preserved. THREE conditioning halves all live: (1) **Qwen2.5-VL VISION tower** (new `Qwen25VlVisionEncoder`, HF `visual.*` keys from the TE file, window-attention ViT + 2×2 merger) feeding vision tokens into the edit chat template via `Qwen25VlMultimodalEncoder` (sectioned M-RoPE [16,24,24]); (2) VAE reference latent appended as frame-1 tokens (was already built); (3) **timestep-zero split modulation** (2511 `index_timestep_zero`: ref rows modulated/gated at t=0 in all 60 blocks). ComfyUI edit-plus recipe: vision ref ~384² area, VAE ref ~1MP area, `Picture 1:` template, dynamic drop. Warm ~93s/20st @1024 F32 (**~1.05× of ComfyUI's 87.8s** — measured same-model baseline; the loop is GPU-compute-bound: 2 CFG forwards × 8400 tokens × 60 blocks × Q5 transient dequant). **Multi-image references** (`44.43-local`): Init Image + Prompt Images (≤3) → `Picture 1..N:` template blocks, per-ref frame-index RoPE, multi-ref VAE latents; verified with a 2-ref composition. Edit loop is drain-free (pinned ref tokens + per-step device concat). **F16 blocked for Qwen** (do NOT re-enable without a BF16 path): residual stream hits ±10M (needs 1/2048 damp), LN eps must scale by damp² for exactness (fixed, kept), but step-2 forwards still NaN deterministically — reference stacks run this model BF16.

### Anima

Clean on-prompt anime @512 on the 3060 (Qwen3-0.6B embeds). **CLI catalog-path verified 2026-07-21**: `hartsy image -m anima --width 512 --height 512` (circlestone-labs/Anima `anima-base-v1.0`), `CliDrivable=true`. ~~**Caveat**: `--width 1024 --height 1024` hung with zero denoise progress after 5 minutes on the 4090~~ — **RESOLVED 2026-07-27, and it was never a hang.** Root cause: `AnimaBlock` ran its entire glue layer as host `float*` loops, costing **792 D2H syncs per step** (= 14 host round-trips × 28 blocks × 2 CFG passes). At 1024² the token count is 4× that of 512², so the same per-step stall simply looked like a hang. Ported to `IBackend` ops following the `Krea2Block` precedent (fused `LayerNormModulate`, `GatedResidualLastDim`, `backend.ApplyRope`, `backend.RmsNorm`) plus an in-place `Backend.CfgEulerStep` at the pipeline level: **3 syncs/step; at 512²/20st warm step 519 ms, generation 12.0 s**. Verified at the exact previously-"hanging" configuration — **1024², the family-default 20 steps**, cfg 4.5 + style-negative, on the **3060**: completes in **50.2s wall-clock, 2.15s/step, 3 D2H syncs/step**, quality-gate clean (1/1) and visually correct (on-prompt astronaut on a horse). The 12 GB card now does at 1024²/20st what the 4090 could not finish in 5 minutes. *(All figures are a Debug build; the ratios are same-build before/after.)* Quality gate clean, statistically matched (PSNR 30.8 dB, FP-reassociation drift, not bit-exact). **No numerical parity coverage** — the Anima layer-diff test was removed in the 2026-08-06 suite cleanup, and it was already skip-guarded (reference tensors absent from this checkout). **Prompt-adherence finding**: at the catalog default `--cfg 1.0` (guidance-free) with no negative prompt, "an astronaut riding a horse" rendered neither subject (two unrelated anime-style figures) — this is NOT a wiring/conditioning bug: a "red apple on a wooden table" sanity check at the same cfg=1.0 DID render a correct red apple (conditioning reaches the transformer), so cfg=1.0 is simply too weak to lock in a multi-subject/out-of-style prompt. Re-tested at `--cfg 4.5` with `-n "anime, illustration, cartoon, abstract"` (seed 42, 512²): clean, correct astronaut-on-horse. Recommend cfg≈4-5 + a style-negative prompt for prompt-faithful (non-anime) generations; cfg=1.0 is the guidance-free anime-native mode.

### Lumina-Image 2.0

Clean on-prompt mountain-lake @512 (53s). Needs the DIFFUSERS-format weights (`Alpha-VLLM/Lumina-Image-2.0` transformer+vae), not the original AlphaVLLM single-file. **Swarm-wired 07-10** (`Lumina2Loader`, live Gemma-2-2B `hidden_states[-2]` encode). **Perf round 07-10 (`44.44-local`): warm 17.7s** — GPU-resident block rewrite + prompt-cached context refiner + rope caches + device glue; coherent, visually verified. Core detection trap: the diffusers transformer matches `isOmniGen` → fix via `EditModelMetadata type=lumina-2`. **CLI catalog-path wiring 2026-07-21** (`hartsy image -m lumina2`): confirmed sharp, on-prompt astronaut-on-horse @1024/30st. Two findings along the way: (1) `Comfy-Org/Lumina_Image_2.0_Repackaged`'s single-file bf16 repack ships the ORIGINAL AlphaVLLM key naming (`t_embedder.*`, no `time_caption_embed` prefix) despite this doc's and `Lumina2CheckpointConverter`'s own claim that "diffusers and ComfyUI single-file distributions both use the same key naming" — verified false by inspecting the file's safetensors header directly; do not use that repack. (2) `Lumina2CheckpointConverter.LoadAndConvert` only ever loaded a single file, so the real 2-shard `Alpha-VLLM/Lumina-Image-2.0` diffusers weights couldn't load either — added multi-shard-safetensors merge support (detects a sibling `*.safetensors.index.json`), changed its return type to `IReadOnlyList<SafeTensorsLoader>` (updated `Lumina2Recipe` and the then-existing generation-test call sites). `CliDrivable=true`.

### Chroma

Clean on-prompt astronaut-on-horse @512 (painterly/ink style). Transformer numerically verified vs diffusers (corr ≥0.999 all components). The earlier noise was a bad experimental checkpoint (symlink → `do_not_use/…exp`); re-pointed. **CLI catalog-path re-verified 2026-07-21** (corrects an earlier insufficiently-scrutinized pass that accepted an off-prompt vintage-rider-in-a-hat image with no astronaut/spacesuit at all — caught on adversarial re-check, same weak compound-subject-binding pattern as AuraFlow): the terse prompt `"an astronaut riding a horse"` at this family's default cfg reproducibly yields that same off-prompt sepia/vintage rider — **not coherent**. Needed an explicit compositional prompt (`"a photo of an astronaut sitting on top of a brown horse in a green field, wearing a white spacesuit and helmet"`) + a negative prompt (`text, watermark, vintage, sepia, old-timey, painting, illustration, cowboy, historical, hat`) + cfg 7.0/30st to reliably bind both subjects — confirmed correct (astronaut in white suit + reflective-visor helmet + flag patch, clearly seated on a brown horse in a green field). `hartsy image -m chroma` now points at `Comfy-Org/Chroma1-HD_repackaged` (`split_files/diffusion_models/Chroma1-HD-fp8mixed.safetensors`, switched from the very slow `silveroxides` host; byte-different, functionally identical fp8-mixed weights). `CliDrivable=true` given the escalated-prompt path is genuinely correct, but the terse default is not — see the catalog comment for the exact prompting requirement. **Multi-GPU (2026-08-05)**: DiT block-range sharding verified — `ChromaDitShardingTests` bit-exact in both regimes, `ChromaDitShardingEngineTests` on real Chroma1-HD fp8 (25/32 split, cfg=5 dual pass, SSIM 0.8797, drift 0.00 GB); note sharding disables Chroma's default-ON step graph (logged — sharding is mutually exclusive with step-graph/step-cache/streaming by design); TE/VAE placement wired (2026-08-04 wave), awaiting checkpoint verification.

### Krea 2 Turbo

Sharp photoreal astronaut-on-horse @1024, 8-step (std 66.5, grid 0.042). Impl fully cross-checked vs ComfyUI `krea2/model.py` (RoPE/modulation/sigmoid-gate attn/text-fusion/scheduler all match). Fix: `Krea2CheckpointConverter` renamed fp8 `.weight` keys but not their `.weight_scale` companions → scales dropped → weights ~250-900× too large → noise; added a scale-suffix pre-pass. Base/CFG path shares the same converter+transformer (untested; CFG anchoring `Krea2Pipeline.cs:100` validation-pending). **CLI catalog-path verified 2026-07-20**: `hartsy image -m krea2` (the `ModelCatalog.Assets`-driven auto-download path, distinct from the `--model-path` verification above) confirmed end-to-end — coherent photoreal fox-in-snow @1024, 8-step; `CliDrivable=true`. **Multi-GPU (2026-08-05)**: DiT block-range sharding verified end-to-end on 4090+3060 (`Krea2DitSharding{,Vram,Engine}Tests`, e2e SSIM 0.8787 vs unsharded, 2026-08-03 — the first sharded diffusion model); TE/VAE placement not yet ported for this family (see MULTI_GPU_COMPONENT_PLACEMENT.md's remaining-unported list).

### Kandinsky 5.0 Lite

Sharp on-prompt snow-leopard-on-peak @512 (std 90, grid 0.084), 64s on 4090. 3 fixes: OOM (12GB BF16 cast to F32 → cast to **F16** instead, fits 24GB), then BLANK (F16 fixed weights-not-applied), then NOISE = **`CudaBackend.LayerNorm` F32-input path didn't cast F16/BF16 affine weights** → text_proj/pooled_proj collapsed to ~0 → conditioning dead (same dtype-mismatch class as GroupNormSilu; GENERAL fix). Block was already GPU-resident. **CLI catalog-path verified 2026-07-21**: `hartsy image -m kandinsky5` (`kandinskylab/Kandinsky-5.0-T2I-Lite-sft-Diffusers`, single-file `transformer/diffusion_pytorch_model.safetensors`, 12GB), sharp on-prompt astronaut-on-horse @1024/50st. `CliDrivable=true`.

### Boogu-Image 0.1 Base

Sharp photoreal astronaut-on-horse @1024-cfg (std 97.8, grid 0.038), **~6 min** (under the 10-min bar). Qwen3-VL-8B final-hidden-state conditioning (Boogu T2I system, no drop) + Flux VAE. Fixes: VAE bare-ldm key remap (`ConvertVaeKey`) + **GPU-residency rewrite of the 8 double-stream blocks** (CPU glue `LuminaRmsNormZero`/`AffineScaleShift` → GPU ops; GPU util 7%→72%; single blocks were already GPU-resident). **CLI catalog-path verified 2026-07-21**: `hartsy image -m boogu` (`Comfy-Org/Boogu-Image`, `boogu_image_edit_fp8_scaled.safetensors`) — deliberately the EDIT checkpoint, confirmed to run cleanly as plain T2I when no reference image is passed (same transformer, edit conditioning slots simply unused); sharp on-prompt astronaut-on-horse-on-Mars @1024/25st. `CliDrivable=true`; `Sha256` left unpinned (local file's hash didn't byte-match the current upload, ~18MB size delta — likely a version bump upstream).

### Boogu-Image 0.1 Turbo

Sharp photoreal astronaut-on-horse @1024, 4-step tg=1.0. Shares Base's config/TE/VAE/converter. Perf grind 2026-07-09 (`alpha.44.22-local`): warm **48.9s→3.26s vs ComfyUI 2.54s (1.28×)**, Base **~6min→26.5s vs 17.8s (1.49×)** — round 1: device RoPE port (OmniGen2Rope.Apply was a host loop draining Q/K per block), cached rope tables + context-refined caption, drain-free CfgEulerStep, KEEP_MODELS + loader TE prompt cache w/ evict staging; round 2: cuDNN head-dim gate widened to admit D=120 (fused flash attention now engages), allowF16 on both block SDPAs, packed-latent loop (bit-equality unit-tested). Swarm routing unblocked via `EditModelMetadata type=boogu` (stale cached metadata — core already had the detector).

### ERNIE-Image

Sharp full-contrast astronaut-on-horse @512-cfg (std 60.9, grid 0.069). 4 bugs fixed: flat-black (BF16-BN-cast NaN), SDPA mask-drop (general fix), VAE banding (non-tiled decode), and the WASHOUT = `CudaBackend.GroupNormSilu` F32 path didn't cast BF16 affine weights → wrong VAE GroupNorm → 4-5× low contrast. Transformer was byte-perfect on a std-ratio diff. See PARITY §ERNIE. Perf grind 2026-07-08 (`alpha.44.19-local`): warm 1024²/20 steps **49.6→~20.3s vs ComfyUI 24.0s** — step-invariant 77 MB attention-mask/RoPE/text-proj caches, masked cuDNN flash SDPA (`allowF16`), drain-free CfgEulerStep loop, TE prompt cache + KEEP_MODELS, device unpatchify/rgb. Seed-777 A/B clean. **CLI catalog-path verified 2026-07-21**: `hartsy image -m ernie-image` (Comfy-Org/ERNIE-Image), `CliDrivable=true`.

### HiDream i1 Dev

Sharp photoreal astronaut-on-horse @1024, verified full 25-step. Residency round 1 (07-10): ~29s/step → **~1.4s/step (≈20×)** — GPU MoE gate+combine kernels (`dit_moe_topk_gate_f32`/`dit_row_gated_accum_f32`), device attention glue (flat-QK-norm caveat: norm precedes head-split), GPU rope + sig-cached Precompute, KEEP_MODELS + quad-encoder prompt cache. **1024²-CFG unblocked 07-11** (activation-memory pass: x_embedder leak fix + 48-latent VAE tiles + post-decode TrimMemoryPool) — official bench row landed on `44.71-local`: **warm 44.0s ×3 flat (25st/cfg5), peak 20.6 GB, coherent** vs Comfy 35.2s = 1.25×. 2 bugs found via numerical diff vs diffusers `transformer_hidream_image.py`: (1) **caption_projection loaded only 2 of 49** (`CaptionChannels.Length`) → every Llama layer through `caption_projection[0]` + T5 through `[1]` = garbage conditioning (brown cloud); fix = load all 49, per-block `caption_projection[i]`, T5→`[-1]` (`t5_proj` relL2 17.86 pinned it). (2) **FFN inner-dim** computed `4·hidden=10240` vs weights 6912/3584 → SwiGLU buffer overflow; fix = derive from `w1.Shape[0]`. GPU-residency block rewrite (52→29s/step). 1024-CFG path functional (smoke-tested). Caveat: intermittent fp8-load flake under memory pressure — retry. **CLI catalog-path verified 2026-07-21**: `hartsy image -m hidream` (Comfy-Org/HiDream-I1_ComfyUI `hidream_i1_dev_fp8`), `CliDrivable=true`.

### OmniGen 2

Coherent astronaut-on-horse @512-nocfg AND @1024-CFG. All 3 bugs (wrong subject, blocky bottom-third, 1024-CFG illegal-address) were ONE root cause: `ComputeFfnInnerDim` used Llama `8/3·dim`=6912 but the checkpoint FFN weight is `4·dim`→10240 → SwiGLU buffer overflow (out-of-bounds GEMM writes corrupted tail image tokens + adjacent memory). Fix = `4·HiddenSize`. Found via numerical diff vs cloned `VectorSpaceLab/OmniGen2` (attn matched 0.009, MLP-out bottom rows 1.0). Precomputed embeds were fine.**Swarm-wired 07-10** (`OmniGen2Loader`, live Qwen2.5-VL-3B encode with ComfyUI-template parity). **First bench 07-10:** warm 90.5s vs ComfyUI 13.0s (7.0×), pre-optimization. **Perf pass 07-18 (`alpha.54→60`): warm 132.6s (re-benched baseline — a regression had crept in) → 19.21s (6.90×), vs ComfyUI 13.0s = 1.48×; coherent, stable, peak 19.5GB.** Levers: device RoPE (host `rope.Apply` Q/K-drain → `WanRopeInterleaved` cached tables; util 29→77%, THE 4× win) + drain-free loop (patchify-once + `ForwardPacked` + `CfgEulerStep` + device unpatchify — removes per-step D2H AND the cpu-glue-async-race crash) + F16 activations/cuDNN-SDPA. **Fixed a cross-model bug:** `DiTUtils.ConcatAlongSeqDim`/`SplitAlongSeqDim` were F32-hardcoded → 2× OOB read on F16 buffers = the intermittent F16 segfault; made dtype-aware. Then device Concat/Split (backend.Concat + SliceRows) → 19.21s, forward now fully device-resident. Remaining path to <13s (project-scale, blocked by variable-length CFG text) = text-padding+masking to enable CUDA-graph the now-all-device per-step forward (`DitStepGraph`) + device Concat/Split. **EDIT path shipped 07-16**: reference-image editing e2e-verified on the 3060 (red apple → green apple @512², 20 st, tg=5/ig=2, table/background preserved; 118s pre-optimization). Transformer grew the full ref stack (`ref_image_patch_embedder` + 2 modulated `ref_image_refiner` blocks + `image_index_embedding[j]` folded into the embedder bias) with upstream `pe_shift` 3-axis positions (`[text, ref_0..N, noise]` joint stream); `EditFromEmbeddings` runs the reference triple-pass dual guidance `pred = uncond + ig·(refPred − uncond) + tg·(cond − refPred)` (cond = pos text + refs, refPred = NEG text + refs, uncond = neg, no refs — shared combine `CfgHelper.ApplyDualCfg`, algebraically identical to Boogu's) + the reference `dynamic_time_shift` (≡ sigma shift `m = sqrt(latent_h·latent_w)/40`) + single-slot ref-latent cache with encode-before-DiT-preload staging. NOTE upstream edit conditioning is TEXT-ONLY through the mllm (vision tokens are the separate chat pipeline's) — refs enter solely as VAE latents. Extension: `OmniGen2Loader` edit branch (Init Image + Prompt Images ≤5, `align_res` output sizing, IP2P CFG 2 → image guidance, default 2.0) — that loader died in the DTO refactor; `ImageRequest.InstructPix2PixCfg` → `imageGuidanceScale` re-wired natively 2026-08-07 (`OmniGen2RecipePipeline`, default 2.0; CLI `--ip2p-cfg`). **CLI catalog-path verified 2026-07-21**: `hartsy image -m omnigen2` (`Comfy-Org/Omnigen2_ComfyUI_repackaged`, `omnigen2_fp16.safetensors`), coherent on-prompt astronaut-on-horse @1024/28st. `CliDrivable=true`.

### Ideogram 4

e2e coherent + prompt-faithful (structured-JSON prompt), visually verified. Perf grind 2026-07-08 (`alpha.44.7-local`): warm 1024²/20 steps **19.5s vs ComfyUI 17.0s (1.15×; was 2.5×)** — step-invariant conditioning caches, cuDNN flash attn @ head_dim 256, `HARTSY_DIT_F16` (o/w3 1/64 sandwich damp), drain-free loop, banded-im2col full-res Flux.2 VAE decode, KEEP_MODELS + prompt cache. **CLI catalog-path verified 2026-07-21**: `hartsy image -m ideogram4` (Comfy-Org/Ideogram-4), `CliDrivable=true` — requires a structured-JSON caption as the prompt (e.g. `{"high_level_description":"...","style_description":{...},"compositional_deconstruction":{"background":"...","elements":[...]}}`); a plain-text prompt is fed verbatim and produces off-prompt output with hallucinated watermark-style gibberish text (the model's own training-data bias, not an engine bug — see Ideogram4Recipe's own runtime warning). **Low-VRAM 2026-07-27**: the recipe's old unconditional ">= 20 GB free VRAM or refuse" gate (both 9.3B DiTs resident for asymmetric CFG) is now enforced **only** when streaming is unavailable (`HARTSY_LOWVRAM=off`, or a non-CUDA backend). `Ideogram4Pipeline` block-streams both transformers via two independent `BlockStreamingController` windows, so the resident set is ~560 MB of shared weights plus the prefetch window. **Verified running on a 12 GB RTX 3060** — 19.7 GB needed (17.1 GB weights + 2.5 GB activations) against 9.2 GB available; completes at 1024²/20st in 205 s with peak **5.1/11.6 GiB** and 0 D2H syncs/step (a per-step pool trim on the streamed path holds it flat; without it the retained stream-ordered pool climbs to the full 11.6 GiB for no speed gain). That old gate is why the 2026-07-26 sweep recorded `OOM²` for Ideogram 4 on the 3060: it never attempted an allocation (1 s, 82 MiB peak) — a policy refusal, not a capacity failure.

### Flux.2 Dev (32B)

✅ **FIRST E2E 07-10** (`44.48-local` + extension): sharp on-prompt astronaut @1024²/20st, warm **52.6s** (peak 23.3GB; step-graph capture OOMs → eager fallback; **ComfyUI baseline 54.37 s (07-18, via city96 GGUF node) → 0.97× ~tied/faster**, `image_python_baselines_2026-07-18.md`). Q4_K_S GGUF native (converter fused-weight splits are now quant-block-aligned via `DType.ComputeByteCount`; loader GGUF branch mirrors QwenImageLoader; BF16 embedders/modulations → F16). Mistral-Small-3 TE live: tekken tokenizer via `ErnieTokenizer` + `ByteLevelCodec` on the official HF tokenizer.json (id-exact vs reference), ComfyUI `[SYSTEM_PROMPT]…[INST]…[/INST]` template spliced as raw special ids, taps [10,20,30], TE↔DiT swap staging. **3 bugs fixed**: (1) loader Qwen-tokenized all variants → Mistral vocab overflow; (2) `MistralSmall3.RopeTheta` was 1e6, real = **1e9**; (3) THE noise bug — a stray **fp4_mixed** TE under the canonical name in `Models/text_encoders/` shadowed the fp8 file (nvfp4→F16 dequant crushes the BOS-row activation outliers → garbage conditioning); canonical name now pinned to `mistral_3_small_flux2_fp8.safetensors`. **Model-switch NRE FIXED — validated 07-11 on `44.71-local`**: Dev→Krea2-Turbo→Dev in one process, all three gens coherent, zero NRE / finalizer-cleanup lines; the switch back INTO Dev also completed (121.8s incl. reload — the feared VRAM-reclaim OOM did not reproduce). Warm 28-st gen 73.9s = 2.64 s/step (same rate as the 52.6s/20-st first e2e; eager loop, step graph opt-in). Benign recurring warning: the 1017 MB VAE-decode alloc logs an async-OOM + sync retry beside the resident DiT, then succeeds. Klein Q4_K_S GGUF also verified coherent. **CLI catalog-path gap found + fixed 2026-07-21**: this GGUF support described above lived only in the (older, separate) SwarmUI extension loader — the native Engine's `Flux2Recipe.Construct` (this repo, `refactor-engine-layout`) had no `.gguf` branch at all and threw a JSON-parse error trying to read a GGUF file's binary header as a safetensors header. Ported the same GGUF-loading pattern already used by `HunyuanImageRecipe`/`QwenImageRecipe` (`GgufModelLoader.Load` + `RelabelRank2ToPyTorchOrder`, `Flux2KeyMapper` already existed and just needed wiring in); also fixed a second bug the port exposed — the pre-existing BF16→F16 cast loop unconditionally cast every non-F32/F16 tensor, which now includes GGUF-quantized (Q4_K) tensors that `Tensor.CastTo` rejects (needs `GgufDequantizer` instead) — narrowed the cast to BF16 only. `hartsy image -m flux2` now verified end-to-end: sharp photoreal astronaut-on-horse @1024/30st (`city96/FLUX.2-dev-gguf` Q4_K_S). `CliDrivable=true`.

### Hunyuan Image 2.1

✅ **FIRST E2E 07-10** (`44.52-local`): sharp 2048²/20st/cfg3.5 astronaut, warm **~50.0s** (**ComfyUI baseline 48.08 s → 1.04× MATCHED**; 07-18 A/B on `alpha.61`: F16 50.1 s ≈ F32 49.9 s. The old **77.0/74.1 s were stale** — prior TE-cache+drain-free work already ground it to ~50 s. **F16-activation opt-in is perf-neutral** here: Q4_K weights dequant to F32 in the GEMM regardless of act dtype; sub-Comfy needs a Q4→F16 dequant-GEMM path (timing only — F16 is numerically UNSAFE on this arch's text stream, see the 2026-07-21 resolution below; since it was already perf-neutral, disabling it cost nothing). `image_python_baselines_2026-07-18.md`). Q4_K_M GGUF native via the new Tencent→diffusers converter + `HunyuanImageLoader` (live Qwen2.5-VL-7B fp8 encode, `.`-token empty-negative guard; ByT5 glyph branch pending). **New engine pieces:** `HunyuanImageVaeDecoder` — the pixel-shuffle VAE (conv→[r1,r2,c]-order depth-to-space + channel-repeat shortcuts, 6 levels, 64-ch latent, scale 0.75289) composed entirely from device ops (2 Permute0213 per rearrange, UpsampleNearest2D as repeat_interleave); **verified relL2 1.1e-4 vs a torch reference**. **THE speckle bug:** final AdaLN-continuous read [shift,scale] but diffusers layout is **scale-first** — structure-correct-plus-uniform-speckle images until flipped (same class as the Qwen final-layer bug). **Perf round 07-11 (`44.71-local`): warm 74.1s** — TE prompt cache (repeat prompt logs a cache HIT and skips the 7B Qwen2.5-VL encode + TE⇄DiT VRAM swap) + token-space drain-free loop; 2048² VAE decode runs clean beside the resident DiT (zero OOM warnings). Model-switch NRE fixed fleet-wide + validated 07-11 (no restart needed between switches anymore). **REGRESSION found 2026-07-21 wiring the CLI catalog path** (`hartsy image -m hunyuan-image`, same `QuantStack/HunyuanImage-2.1-GGUF` Q4_K_M this doc references): output was all-zero (black), and 2048² additionally OOMs at the final VAE `Upsample`/`ShuffleRearrange` step even on an otherwise-idle 4090 (this doc claims "zero OOM warnings" at 2048² previously) — 1024² produced the same all-black result, ruling out resolution as the sole cause. Fixed one real, still-worth-keeping latent bug along the way: `GgufKeyMapperRegistry`'s fallback heuristic registered `FluxKeyMapper` (matches on bare `double_blocks.`+`single_blocks.` presence) before `HunyuanImageKeyMapper`, so any GGUF whose `general.architecture` isn't registered (this file declares `hyvid`) could silently resolve to the wrong mapper — reordered so HunyuanImageKeyMapper is checked first. This did **not** fix the black-image symptom (both mappers' `MapKey` are no-op passthroughs, so the reorder was inert for this specific file). **ROOT-CAUSED + FIXED 2026-07-21**: per-layer tensor-stats tracing (`HunyuanImageTransformer.Forward`/`HunyuanImageSingleBlock.Forward`) isolated the NaN to the CFG-conditional branch only — the unconditional (near-empty, 1-token) branch always stayed finite, while the real-prompt (6-token) branch went all-NaN starting at `single_transformer_blocks.0`'s joint self-attention. Traced further: the poison was a single **+Inf** already present in the joint `[img,txt]` tensor BEFORE that block ran — the **text** stream (not image) overflows F16's 65504 ceiling by the last (20th) double-stream block. HunyuanImage's Qwen2.5-VL conditioning feeds an UN-normalized middle-layer hidden state (`hidden_states[-3]`) that legitimately reaches residual-stream magnitudes in the thousands (measured ~3000 at the context_embedder output, growing to ~5×10⁵ by double block 19 for a real prompt) — F32 handles this fine, but the engine's shared 16-bit DiT hot path (`HARTSY_DIT_F16`, opt-in per-model) does not: one Inf'd text token poisons the entire joint softmax in single-block 0 (every image query attends to the same key/value set), cascading to an all-NaN velocity → all-NaN latent → solid-black image. The short empty/negative CFG prompt (1 token, small magnitude) never reached the overflow, which is why only the conditional branch broke. **Fix**: `HunyuanImageTransformer.Forward` no longer opts into the shared 16-bit hot path (`DiTBlocks.DitDtype.CastStreamToAct`) — its block loop runs at F32 unconditionally (BF16 was considered — same exponent range as F32, would dodge the overflow while keeping a 16-bit activation — but the shared `LayerNormNoAffine`/`AffineBroadcastLastDim` CUDA kernels this arch's `NormModulate` depends on only accept F32 or F16 today; adding BF16 there is real follow-up work touching kernels shared by every other DiT, out of scope for this fix). Verified: 20-step 1024² astronaut-on-horse gen is coherent and on-prompt, zero NaN/Inf end-to-end. `CliDrivable=true`. **Low-VRAM 2026-07-27**: `HunyuanImageTransformer` now exposes `BlockCount`/`GetBlock`/`BeforeBlockForward` and a shared-vs-block weight split, so `HunyuanImagePipeline` streams its 60 blocks when the resident layout will not fit (its two block classes already implemented `IStreamingBlock`, but nothing had ever exposed them). **Verified running on a 12 GB RTX 3060** at 1024², 19.7 s, quality-gate clean — 11.5 GB needed vs 9.4 GB available, so it does *not* fit resident. The 2026-07-26 sweep's `OOM²` for this model on the 3060 is superseded. Note the block-size fix that made this possible: `Q4_K.SizeInBytes` is 0, so the pre-fix budget measured this GGUF's blocks as **0 bytes** and always chose the eager path. **Multi-GPU (2026-08-05)**: DiT block-range sharding synthetic-verified bit-exact (`HunyuanImageDitShardingTests` green); the real-weight engine e2e (`HunyuanImageDitShardingEngineTests`) is written but awaiting the Q4_K_M re-download; TE (both encoder paths) + VAE placement wired (2026-08-04 wave, incl. the explicit host bridge for the device-resident latent→VAE chain), awaiting checkpoint verification.

### Boogu-Image 0.1 Edit

✅ **07-16 e2e (engine main + extension fix)**: instruction edit verified on the real fp8_scaled checkpoint (apple → yellow pear, scene identity preserved; 768², 28 steps, tg=4/ig=1, seed-exact repro of the live failure then the fix). ROOT CAUSE of “instruction ignored, pixel-perfect reconstruction”: the loader fed the Qwen3-VL VLM the native-res init image (up to **1024 merged vision tokens**) — the reference caps the VLM input at **384·384 px / side ≤768 (≈144 tokens, its training budget)**; at 1024 OOD tokens the DiT ignores the text at any cfg. Extension now pre-resizes the VLM copy, encodes the negative WITHOUT the image (`use_input_images_4_neg_instruct=False` reference default), and stages TE ⇄ DiT (evict + host-materialize embeds) — encode 37.7→3.7s. Qwen3-VL vision tower real-weight parity corr 1.000000 vs HF (CPU + CUDA); see BOOGU_IMAGE.md §5/§7. Harness: the Boogu edit end-to-end test, removed 2026-08-06.

### Flux.1 Kontext

✅ **07-16 e2e (`49.6-local`)**: clean instruction edit (apple recolor, composition/identity preserved). THE texture-corruption root cause: `VaeEncoder` stride-2 downsample used symmetric padding where diffusers/BFL use asymmetric `(0,1,0,1)` — encode corr vs ComfyUI 0.871 → **0.999993**. Kontext exposed it (conditions on the ref latent verbatim); img2img always masked it. Fix benefits ALL image-conditioned flows (img2img/inpaint/Fill/Tools, SD15/SDXL/SD3/Flux). PHASE_3_DEVIATIONS #45.

### Flux.1 Canny

✅ **07-16 e2e (`49.1-local`)**: full pipeline (edge map → VAE → 128-ch concat) verified — marble apple following the reference contour exactly (guidance 30, BFL-recommended). ROOT-CAUSE FIX: extension CannyPreprocessor produced near-EMPTY maps on real photos (gaussian σ1.4 pre-blur crushed gradients below the 200 threshold + L2 vs cv2's L1 magnitude); removed blur + L1 → cv2.Canny(100,200)-class maps. Debug aid: `HARTSY_DUMP_CONTROL=dir` dumps the conditioning map PNG.

### Flux.1 Depth

✅ **07-16 e2e (`49.4-local`)**: depth-faithful gens (synthetic-map ball test + cropped-reference apple, both clean). The "web junk" was flux1-depth MODEL behavior on depth maps containing flat caption-strip-like bands (this test photo's table front face) — not a bug; both the Academia fp8 and an F16 conversion behave identically. In-engine **Depth-Anything-V2** annotator (DINOv2 reuse + DPT head, parity 2.9e-7 rel; ViT-L auto-download; `fluxScaling` max-only option matches BFL's reference form). **07-17 fringe A/B closed (Python references)**: the conditioning map is NOT the fringe source — full-path parity on a real photo corr 1.000000 at every stage incl. a dilated-Canny edge band (see PARITY_VERIFICATION.md); flux-mode upsample switched to BFL's exact bicubic-antialias kernel (map now corr 1.000000 / maxAbs 5.1e-5 vs the BFL reference; live re-verify recommended on next deploy). Residual fringe, if still visible, is generation-side (fp8 DiT at guidance 10). Guidance 10 recommended (BFL).

### Flux.1 Redux

✅ **07-16 e2e (`49.1-local`)**: new engine `ReduxImageEncoder` (redux_up/silu/redux_down) + `SiglipVisionEncoder.EncodeHiddenStates` + FluxPipeline seq-append (+ per-step apply-start switch); extension `ReduxResolver` reads Comfy's `usestylemodel` + strength params (multiply×merge both reduce to a scalar on the redux tokens — derived + documented), SigLIP so400m/14-384 side-model auto-download. Swarm verify: reference-dominant image variation (expected Comfy `StyleModelApply` behavior at high strength; scale knob confirmed applied). **07-17 numeric A/B closed**: encoder+projector corr 1.000000 vs the diffusers/BFL math on shared pixels; library SigLIP preprocessor fixed to HF-parity AA-bicubic (end-to-end tokens corr 0.999828); multiply/merge semantics source-verified ≡ Comfy (see PARITY_VERIFICATION.md).

### Lance (ByteDance) image

>` slots even for images; noise split includes the vision sentinels). Sampler: cfg_interval (0.4,1] + global CFG renorm. Parity vs upstream code + real weights (CPU F32, 36 layers, seq 309): **pos_ids exact, packed_in 1.4e-7, all layers avg ≤6.7e-4, velocity avg 1.38e-6 / corr 1.000000** (`dump_lance_reference.py` + `diff_lance_layers.py`; the C# parity test was removed 2026-08-06); tokenizer template segments byte-exact vs HF (via `HfTokenizerJson`/`GgufTokenizer` — the two-file BPE mis-split ` "` and is fallback-only). **CLI catalog-path verified 2026-07-21**: `hartsy image -m lance-image` (`bytedance-research/Lance`, `Lance_3B/model.safetensors`), sharp on-prompt astronaut-on-horse @1024/30st. `CliDrivable=true` (catalog `Status` also corrected from stale `vp` to `ok` — this row already documents real-weight parity).
