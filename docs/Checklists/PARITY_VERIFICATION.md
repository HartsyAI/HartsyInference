# Model Parity Verification — master checklist

> One living checklist for **every** model HartsyInference targets (all modalities), tracking the
> **numerical parity verification loop**: load the real checkpoint → dump a reference from the upstream
> implementation → diff the C# output → fix bugs → check it off. This is distinct from the per-modality
> status docs indexed in [`MODEL_STATUS.md`](MODEL_STATUS.md) (which track whether a model is *built*);
> this tracks whether it has been *proven correct against real weights*.
>
> **Per-type status docs:** [Image](MODEL_STATUS_IMAGE.md) · [Audio](MODEL_STATUS_AUDIO.md) ·
> [Video](MODEL_STATUS_VIDEO.md) · [World](MODEL_STATUS_WORLD.md) · [3D](MODEL_STATUS_3D.md) ·
> [Vision](MODEL_STATUS_VISION.md) · [LLM](MODEL_STATUS_LLM.md).

---

## FOR THE AGENT — how to verify a model (read this first)

You are continuing a long-running effort to fix and numerically verify every model in this engine. Pick the
next unchecked model (or one the user names) and run the **parity loop**. When done, check it off here and add
the bugs you found + how you found them.

### The parity loop (the method that has worked for every model below)
1. **Map the C# side.** Read the model class(es) + `LoadWeights` + the pipeline. Note the exact weight-key
   strings it expects and the config dims. (An `Explore`/general-purpose subagent is good for this.)
2. **Get the real checkpoint.** `hf download <repo> <file> --local-dir <scratchpad>`. Many are gated — ask the
   user to accept terms / provide an HF token.
3. **Inspect the real keys vs what C# expects.** Dump the safetensors/pickle header
   (`struct.unpack('<Q', f.read(8))` then the JSON). The #1 source of bugs is a key-layout / architecture
   mismatch (the C# scaffold guessed wrong). Reconcile before anything else — `LoadWeights` hard-throws on the
   first wrong key.
4. **Extract the upstream reference.** Prefer running the real upstream library (HF `transformers`,
   `diffusers`, `moshi`, the model's repo) as an oracle. `pip install` it into the sandbox; if its env is
   broken, write a standalone torch reimplementation from the source. Dump fixed-input → fixed-output IO to a
   `.safetensors` the C# test reads.
5. **Diff per-component, then end-to-end.** Validate the *risky* pieces in isolation first (novel attention /
   RoPE / norm / fused-weight layouts), each against its own reference dump, THEN the full forward. Bugs are
   far easier to localize per-component.
6. **Fix → re-run → tighten tolerance.** See tolerance table below.
7. **Check it off here** with the maxAbs achieved, the bugs found, and how.

### Reference-dump gotchas (learned the hard way)
- **Memory safety — CRITICAL.** Loading a multi-B model cast to f32 (`EnsureF32`) can be 5–13 GB RAM. Doing
  this while VSCode + another agent run **OOM-killed the box and crashed VSC**. Rules: **never `.float()` a
  whole large model in the Python reference** — cast only the submodule under test (`lm.transformer.float()`).
  Guard every C# test run: abort if `free < 2500 MB`; run **one heavy test at a time**; prefer **CUDA**
  (weights stay host-resident, ~1.5 GB VRAM, RAM only holds the f32 cast).
- **Sandbox torch is CPU-only.** It can't bf16-matmul → cast the reference to f32 (this is the reference, not
  the 3060). Disable torch.compile: `TORCHDYNAMO_DISABLE=1 TORCHINDUCTOR_DISABLE=1` (inductor's C++ compile
  fails in the sandbox).
- **bf16 reference noise** accumulates ~1e-2 over many layers; for a clean f32-vs-f32 check, cast the
  reference submodule to f32 and you'll see ~1e-4.
- **CPU is slow / dangerous for big AR models.** An O(n²) full-prefix re-run on a 1.6 B model never finished on
  CPU and pegged the CPU. Use the GPU; add a KV cache for AR generation.

### Tolerance conventions (PHASE_5_AUDIO §9 + practice)
- Linear / attention F32 vs f32 reference: **~1e-4** (≤5e-3 passes). bf16 reference: ~1e-2.
- Logits over many layers with large magnitude: **~1e-2 abs** can be expected (depformer logits hit 6.9 peak).
- **Argmax/codes (greedy):** prefer **exact token match** as the functional check, but know that bit-exact
  greedy over a *long* sequence is fragile — a validated 1e-4 backbone error eventually flips one argmax and
  greedy cascades it. For long AR, validate **logits under teacher-forcing**, not sampled codes.
- Codec round-trip: STOI > 0.95.

### How to record results here
Mark the **Parity** column: ✅ verified vs real weights · 🔬 partially verified (some components) ·
🔧 built, parity-pending · 🚧 scaffold only · ❌ not started · ⛔ blocked (gated weights / external dep).
Add a row to **§ Bugs found** for every real bug, with how it was caught.

---

## AUDIO

### TTS
| Model | Parity | Components verified (maxAbs) | Notes |
|---|---|---|---|
| **Kyutai TTS** (tts-1.6b-en_fr) | 🔬 | backbone 1.3e-4 · depformer 32/32 tokens · conditioner ~1e-8 · forward_text 2.6e-6 · scheduler exact · KV-cache StepForward 1.3e-4 | All numerical cores + loop logic verified. Greedy end-to-end codes diverge after cb-0 by **numerical argmax cascade** (not a bug). Mimi DSM decode = separate codec reconcile (in progress). See [[kyutai-tts-reconcile]]. |
| **GPT-SoVITS v2** | ✅ | HuBERT 1.07e-5 · s1 GPT (golden-fixed) · s2 SoVITS (3 goldens) · EN end-to-end → 32 kHz | English path end-to-end on real `lj1995` weights. See [[gptsovits-v2-validated]]. |
| **Chatterbox** (ResembleAI) | ✅ | S3Gen enc 2.6e-6 · dec 4.4e-5 · vocoder 1.6e-5 · perceiver 1.5e-6 · T3/VE/tokenizer | Full S3Gen rewrite (== CosyVoice2); end-to-end on CUDA. Supersedes the old "not real-weight validated" note. |
| **Qwen3-TTS** | ✅ | bit-exact | RoPE split-half + byte-level tokenizer fixes. See [[qwen-tokenizer-bytelevel-bug]]. |
| **Piper** (VITS) | ✅ | corr 0.9998 vs onnxruntime | 7 VITS bugs fixed (affect all VITS). See [[piper-vits-parity]]. Phonemization (espeak) is the only gap. |
| **Kokoro** (StyleTTS2) | ✅ | ~1e-4 (CUDA path) | Added `audio_leaky_relu`/`audio_adain1d` CUDA kernels. Vocoder DSP is pure C#. See [[kokoro-cuda-path-complete]]. Loader 404s until repacked — see [[audio-repack-tool]]. |
| **CosyVoice 2** | ✅ | == Chatterbox S3Gen (shared) | Validated via the Chatterbox rewrite. |
| **F5-TTS** (v1 Base) | ✅ | DiT velocity corr 1.0 (maxAbs 2e-5) · full CFM sample-loop generated mel corr 1.0 (maxAbs 7e-5) · Vocos corr 0.9999 | Flow-matching DiT + Sway-Euler + cond-anchored CFG, all bit-exact vs the upstream `DiT`/`CFM.sample`. See [[f5tts-build]]. |
| **HeartMuLa** (oss-3B) | ✅ | **LM**: backbone+c0 head argmax exact (corr 0.9996) · depth decoder cb1–7 corr 0.9998 (6/7 argmax; the 1 flip = bf16 tie). **HeartCodec** (rewritten this session): flow-match **estimator corr 1.000000 (maxAbs 1e-5)** + **ScalarModel waveform decoder corr 1.000000 (maxAbs 2e-5)** vs `heartlib` (both 0-missing keys); full decode → finite 48 kHz audio. Runs correctly on **CUDA** too (estimator corr 1.0 vs CPU; full decode 89 s vs 150 s CPU). | LM (CSM-shaped, torchtune llama3_2) reconciled via `HeartMulaCheckpointConverter`. **HeartCodec rewritten to the real arch** (`HeartCodecRvq` + `HeartCodecEstimator` = 24×1536+6×3072 adaLN-single DiT + `HeartCodecScalarModel` SQ-Codec, 818 keys); 2 bugs fixed (see below). RVQ embed-lookup + cond Linear + nearest-upsample + deterministic Euler+CFG loop are trivial → full decode verified by composition. Codec uses manual host loops → CPU/CUDA both run; full GPU-residency (manual loops → backend ops) is a perf-only follow-up. Probes: `/tmp/codecprobe` (estimator/ScalarModel/CUDA parity), `/tmp/hmprobe` (LM); oracle `/tmp/heartlib`. |
| **ResembleEnhance** | 🔬 | modules synthetic-verified; checkpoint converter built | Mel→mel structural green; real-weight parity pending. |
| **Spark-TTS-0.5B** | ✅ | LM logits corr 1.0 (top-1 100%) · greedy tokens 32/32 global + 179/179 semantic · BiCodec z_q/d_vector/prenet/wav all corr 1.0 | Fully in-engine controllable mode (`SparkTtsPipeline.SynthesizeControllable`). BiCodecDecoder rewritten (factorized VQ, FSQ d-vector flatten, AdaLN PreNet); reuses `DacDecoder` (added dim-0 weightnorm + explicit-kernel opt-ins). `SparkTtsTokenizer` reuses BpeTokenizer+ByteLevelCodec. See [[sparktts-build]]. |
| **MeloTTS** (English-v3) | ✅ | g2p ids exact · BERT features rmse 0.0 · text-enc + DP corr 1.0 · audio corr 0.9993 (len exact) | Real-weight e2e in pure C#. `MeloTts` facade (LoadFromFiles/LoadAsync/SynthesizeText). Built VitsFftBlock + VitsTransformerFlow. See [[melotts-build]]. |
| **FishSpeech 1.5** | ✅ | DualAR LM verified: slow (24-layer) logits corr 1.0 · fast depth-LM (4-layer) corr 0.9999. Firefly-gan-vq codec DECODE corr **1.000000** · maxAbs **5.0e-6** vs fish-speech v1.5.1 (real `firefly-gan-vq-fsq-8x1024-21hz-generator.pth`), every decode sub-stage bit-exact (FSQ dequant → ConvNeXt upsample → SiLU HiFi-GAN). | LM done. CODEC REWRITTEN to the real arch: `FireflyOps` (FishConvNet causal-constant-pad conv + FishTransConvNet right-trim transpose conv + weight-norm fuse handling `parametrizations.weight.original0/1`), `FireflyConvNeXtBlock` (depthwise conv → channels-last LayerNorm → GELU(erf) MLP → LayerScale → residual), rewrote `FireflyQuantizer` (grouped FSQ: 8 groups × `project_out` 4→64, concat → 2× ConvNeXt upsample) and `FireflySiluGenerator` (conv_pre → 5× [silu, FishTransConvNet up, ParallelBlock=mean of 3 ResBlock1] → conv_post → tanh; `resblocks.{i}.blocks.{k}` nesting, convs2 shares branch dilation). Two bugs found: (1) the entire wrong-arch layout (expected nonexistent `fc_post`/flat keys); (2) the FSQ index→code uses `preserve_symmetry=True` (`code = digit·2/(L-1) − 1`), NOT the `half_width=L//2` convention in `Codecs.Fsq`. Oracle `dump_firefly_reference.py` + per-stage `dump_firefly_stages.py` (vendored `fsmod/`); tests `FireflyCodecParityTests` + `FireflyStageDebugTests`. |
| **Dia-1.6B** | ✅ | full transformer corr 1.0: encoder (12L) + decoder (18L, cross-attn / 9-ch / fused head, teacher-forced); DAC-44k decode corr 1.0 (this session, `DacParityTests`); delay apply/revert unit-tested; `DiaPipeline.Generate` wires the full 9-channel delay-AR + DAC decode. Verified by composition (only the stochastic AR sampling is non-bit-matchable, as for every TTS). | DenseGeneral transpose adapter + split-half RoPE + attn scale 1.0 + `DacDecoder`-style mlp/logits rename + KV-cache `AdvanceLength` fix. Only DAC wiring (shared/✅) + delay-AR remain. See [[dia-fishspeech-build]]. |
| **Orpheus 3B** | ✅ | LM backbone (Llama-3.2-3B → `Qwen2Model`/`GenericTransformer`) teacher-forced logits **argmax 12/12 exact** · corr **0.9986** vs `LlamaForCausalLM` (unsloth/orpheus-3b-0.1-ft, ungated mirror) on **CUDA** (3060, bf16 GPU-resident); SNAC codec decode already ✅. End-to-end verified (only stochastic sampling is non-bit-matchable). | bf16 reference (CPU bf16 matmul, ~6 GB) → ~1e0 abs logit noise over 28 layers is expected; the greedy-argmax exact match is the functional gate. Config (3072/28/24/8 GQA/8192/vocab 156940/RoPE θ500000 + **llama3 NTK scaling factor 32**) confirmed against the real `config.json`; `RopeScaling` IS wired through `Qwen2Model`→`GenericTransformer` (the "not applied" notes were stale). C# loads HF safetensors shards directly; `CudaBackend(0, ptxDir)` + `HARTSY_LOWVRAM`. Oracle `tests/python-reference/orpheus_reference/`; test `OrpheusParityTests` (gated on CUDA + `ORPHEUS_DIR` + `ORPHEUS_REF`). Proves the GenericTransformer Llama path on a 3B model on the 3060 → unblocks CSM/Bark. |
| **Bark** (suno) | ✅ | All 3 GPT stages teacher-forced logits **argmax exact** vs `transformers` BarkModel (suno/bark): semantic 6/6 (corr 0.99992), coarse 6/6 (corr 1.0), fine cb=2 6/6 (corr 0.99993); EnCodec-24k codec decode already ✅. End-to-end verified (only stochastic sampling non-bit-matchable). | 3 bugs fixed: (1) **bias-free LayerNorm** — HF `BarkLayerNorm` uses `bias=False` (only `.weight` keys), but `GptBlock`/`GptBackbone` required `.bias` → KeyNotFound; now zero-bias fallback (`GptBlock.LoadBiasOrZero`). (2) fine stage key `input_embeds_layer`→`input_embeds_layers` (plural). (3) fine embed-sum off-by-one — HF sums codebooks `[0, codebook_idx]` **inclusive** (the predicted slot holds a 0 placeholder); `Refine`/`DebugLogits` now sum `pred+1`. Also noted: fine `lm_heads.{i}` are weight-tied to `input_embeds_layers.{i+1}`. Oracle `tests/python-reference/bark_reference/`; test `BarkParityTests`. |
| **VibeVoice / NeuTTS / StyleTTS2** | 🔧 | — | Built (varying completeness); no real-weight parity yet. NeuTTS is phoneme-id-blocked (caller supplies ids). |
| **EnCodec 24 kHz** (Bark codec) | ✅ | DECODE corr **1.000000** · maxAbs **1.4e-5** vs `transformers` EncodecModel (facebook/encodec_24khz), every SEANet decoder stage bit-exact (per-stage dump). | Root cause was NOT the causal path: the 24/48 kHz checkpoints set `use_conv_shortcut=True` so every SEANet residual block has a **learned 1×1 conv on the skip path** (`...shortcut.conv.conv.*`), but `SeaNetBlock` hardcoded an identity skip → divergence began exactly at the first ResnetBlock (stage_04 corr 0.55) while the initial-conv/LSTM/transpose stages were bit-exact. Fix: `SeaNetBlock` now auto-detects the shortcut by checkpoint key presence and applies `Conv1d(dim→dim,k=1)` to the residual; absent keys (32/16 kHz) keep the validated identity skip. Oracle `dump_encodec24_reference.py` + per-stage `dump_encodec24_stages.py`; tests `EnCodec24ParityTests` + `EnCodec24StageDebugTests`. |
| **DAC 44.1 kHz codec** (Spark/Dia/IndexTTS/Higgs) | ✅ | decode corr **1.000000** · maxAbs **6.4e-7** vs a torch oracle on the real `descript-audio-codec` 44 kHz weights (CPU f32) | Standalone confirmation of the factorized-RVQ decode + snake decoder + final tanh (previously only validated indirectly via Spark BiCodec). Test `DacParityTests` (gated `DAC_WEIGHTS_PATH` + `DAC_REF_IO`), oracle `tests/python-reference/dac_reference/`. |
| **SNAC 24 kHz codec** (Orpheus vocoder) | ✅ | decode corr **1.000000** · maxAbs **9.8e-6** vs a torch oracle on the real `hubertsiuzdak/snac_24khz` weights (CPU f32) | Rewrote the codec to the official `layers.py`: `depthwise=true` initial split (`model.0` depthwise + `model.1` pointwise), `NoiseBlock` at `block.2` (residual units at `block.3+`), residual `groups`, final `Tanh`; encoder final-conv layout (no final Snake) confirmed against the real keys. `Snac.LoadWeights` maps the torch>=2.1 `parametrizations.weight.original0/1` format via `CodecKeyUtils`. NoiseBlock is stochastic in the reference; parity uses `NoiseScale=0` (oracle stubs `torch.randn`). Test `SnacParityTests` (gated on `SNAC_WEIGHTS_PATH` + `SNAC_REF_IO`), oracle `tests/python-reference/snac_reference/dump_snac_reference.py`. 32/44 kHz LocalMHA still pending. |
| **Zonos** | 🔧 | — | NOT blocked on espeak: the engine has a full pure-C# espeak-ng port (`HartsyInference.Phonemizer/Espeak`, wired via `ZonosConditioning`). Real remaining work: ResNet293 speaker encoder real-weight parity, the NovelAI/unified sampler, and the hybrid Mamba2 variant. Weights ungated (`Zyphra/Zonos-v0.1-transformer` + `-hybrid`). |

### STT
| Model | Parity | Components verified | Notes |
|---|---|---|---|
| **Whisper** (tiny→large-v3) | ✅ | JFK clip transcribes correct content words | `WhisperEndToEndTests`. |
| **Whisper streaming** (RealtimeSTT) | ✅ | LocalAgreement-2 + JFK streaming | Built this milestone. |
| **Moonshine** | ✅ | tests pass | — |
| **Kyutai STT** (stt-1b/2.6b) | 🔧 | — | Shares the moshi backbone; parity pending (no depformer). |

### Codec / VC / music / separation
| Model | Parity | Components verified | Notes |
|---|---|---|---|
| **Mimi** (codec) | ✅ | full DECODE corr **1.000000** / maxAbs **2.1e-6** vs `transformers` MimiModel on real `kyutai/mimi` (split-RVQ corr 1.0 / 3.5e-6 standalone) | Rewrote to the HF layout (the scaffold had invented keys): new `MimiSplitRvq` (semantic+acoustic EMA codebooks, `embed = embed_sum/clamp(cluster_usage)`, dual output_proj), depthwise `upsample` ConvTranspose (k4/s2/groups512), `MimiTransformer` fixed to LayerNorm + LayerScale 0.01 + sliding-window 250 + split-half RoPE + exact-GELU (HF keys `input_layernorm`/`self_attn.q_proj`/`mlp.fc1`), new `MimiSeanetDecoder` (pre-fused `decoder.layers.N.conv`, ratios [8,6,5,4], ELU, causal). Decode order quantizer→upsample→transformer→decoder. Tests `MimiRvqParityTests`; oracle `tests/python-reference/mimi_reference/`. Encode (audio->codes) is a follow-up. Unblocks Kyutai STT/TTS + CSM. |
| **OpenVoice** (tone-color VC) | ✅ | Conv2d + GRU validated; speaker encoder | See [[audiolab-engine-blockers]]. |
| **CAM++ / CamPlus** (speaker) | ✅ | from `funasr/campplus_cn_common.bin` | — |
| **S3Tokenizer** | ✅ | from `s3tokenizer` pkg | — |
| **Vocos / Vocoders** | ✅ | test passes | — |
| **ACE-Step v1** (music DiT 3.5B) | ✅ | DiT ~1e-8; DCAE decoder corr 1.0; vocoder corr 1.0 (all numpy-ref parity, every tap) | Full e2e on **CUDA/3060** (bf16 + `CudaBackend.HighPrecisionGemm`) writes finite audio — `Generate_OnCuda_HighPrecision_WritesWav`; CPU e2e env-gated. Parity tests: `AceStep{Dit,Dcae,Vocoder}DiffTests` (numpy refs in tests/python-reference). See [[ace-step-build]]. |
| **ACE-Step v1.5 turbo** (music DiT 2B) | ✅ | **condition encoder maxAbs 1.9e-6** · **DiT velocity maxAbs 5.1e-6 (corr 1.0)** · **full 8-step shift-3 turbo Euler loop final latent maxAbs 6.6e-6 (corr 1.0)** — all CPU F32 vs a standalone f32 torch oracle on the **real Comfy-Org turbo safetensors (677 tensors)**. Oobleck VAE decode separately verified corr 0.9999999999. | The Qwen3-block flow-matching DiT (`AceStep15Dit`/`AceStep15ConditionEncoder`) is numerically correct **as built — zero C# bugs found**: RoPE (split-half, θ=1e6), q/k-norm, AdaLN-6 modulation order, un-roped cross-attn, dual time-embed (`t` + `t−r`, cos-first ×1000 sinusoid), Conv1d patch-embed / ConvTranspose de-patchify, alternating sliding(128)/full bidirectional masks, and the SwiGLU MLP all match. The turbo scheduler (`AceStep15Config.GetTimesteps` + the Euler `z += (σ_next−σ)·v` update, σ_next=0 on the last step) is **bit-identical to the oracle by construction**. Full e2e already runs on **CUDA/3060** (bf16 + `HighPrecisionGemm`) writing finite tonal stereo (rms 0.20). Permanent test `AceStep15DitParityTests` (gated on `ACESTEP15_DIT`; refs vendored in `tests/python-reference/acestep15_ref`, oracle `dump_acestep15_reference.py`). |
| **MusicGen** (small) | ✅ | T5-base 1.9e-6 (corr 1.0) · decoder logits teacher-forced corr 0.999999 (maxAbs 1.5e-2, 4 codebooks) · EnCodec-32k decode corr 1.0 (maxAbs 1.4e-6, every SEANet stage bit-exact) | Full e2e on CUDA/3060 writes finite music-like audio (rms 0.23, spectral flatness 0.20 vs 0.56 white-noise baseline). Parity tests `MusicGenParityTests` (T5/decoder/EnCodec) + ref dump `dump_musicgen_reference.py`. 5 bugs fixed (see below). AR loop is O(n²) full-prefix (no KV cache) → slow but correct. See [[musicgen-build]]. |
| **YuE** (music, Stage-1) | ✅ | **Stage-1 7B LM**: teacher-forced logits maxAbs **2.5e-5**, **corr 1.0**, **argmax 8/8** vs a Llama-2-GQA oracle (CUDA+`HighPrecisionGemm`, bf16). **XCodec decode** (rewritten this session): **corr 1.000000 (maxAbs 2e-6)** vs the upstream SoundStream decode path (manual RVQ embed-lookup → `fc_post2` 1024→256 → `dac2.Decoder`; decoder loaded 0-missing) → finite 16 kHz audio. | LM (`m-a-p/YuE-s1-7B-anneal-en-cot`) reconciled: GQA `num_key_value_heads=4`, vocab 83968 (were 32/100000); tokenizer markers are SP control symbols `<SOA>`/`<EOA>`/`<stage_1>` (pinned-id + Decode round-trip). **XCodec rewritten** to the real SoundStream EMA-VQ-12 arch (`XCodecEmaResidualVectorQuantizer` + `fc_post2` + dac2 decoder); `YueCheckpointConverter.LoadXCodec` loads `ckpt_00360000.pth['codec_model']` (converted to safetensors). YuE Stage-1 emits vocal cb0 only; Stage-2 (multi-codebook) intentionally out of scope. Probe `/tmp/codecprobe` (XCodec parity) + `/tmp/yueprobe` (LM); ref `models/soundstream_hubert_new.py`. |
| **RVC** (voice conversion) | 🔬 | **RMVPE** pitch extractor (E2E network) hidden corr **1.000000** · maxAbs **9.5e-8** vs RVC-WebUI `E2E(4,1,(2,2))` on the real `rmvpe.pt` (mel→360-bin posterior; cnn/flat/BiGRU/hidden all bit-exact). | RMVPE was a wrong-arch scaffold (GroupNorm/strided-conv/single-conv/BiLSTM/missing `cnn`); **rewritten** to the real DeepUNet: BatchNorm2d (folded to per-channel affine) + 5× ResEncoderBlock (4 ConvBlockRes + AvgPool2d) + 4 intermediate blocks + 5× ResDecoderBlock (ConvTranspose2d k3 s2 outpad1 + skip-concat) + `cnn` 16→3 + **bidirectional GRU** (forward + reversed-pass over `*_l0_reverse` keys) + Linear 512→360 + sigmoid. Real keys `unet.{encoder,intermediate,decoder}` / `cnn` / `fc.0.gru` / `fc.1`. Oracle `tests/python-reference/rmvpe_reference/` (vendored `rmvpe_model.py`, 0-missing-key load); test `RmvpeParityTests`. RVC flow/decoder + the mel-front-end f0 decode still pending. |
| **Demucs** (htdemucs v4) | 🔬 | STFT + complex-as-channels spec front-end (`DemucsSpec.Spec`, the bit-exact-risky piece) corr **1.000000** · maxAbs **8.2e-8** vs demucs `HTDemucs._spec` on real htdemucs weights. | 2 bugs fixed: (1) **STFT `center=True`** — demucs `spectro` runs `torch.stft(center=True)` (an extra `nfft/2` reflect-pad each side) ON TOP of `_spec`'s manual reflect-pad; the C# did only the manual pad + `center=False` → corr ~0. Now replicates the two successive reflects then trims `z[...,2:2+le]`. (2) channel (up/down)samplers are **top-level** HTDemucs modules, not under `crosstransformer.` (key-prefix fix). Full-model e2e was TESTED (`DemucsFullParityTests`, oracle with `use_train_segment=False`) and the C# `HtDemucs`/`DemucsConvBlock` is a **staged scaffold**, not yet a parity build: (a) it **omits the DConv residual branches** entirely (docstring: "DConv residual branch are off ... staged") that every real HEnc/HDecLayer has (`dconv.layers.{0,1}.{0,1,3,4,6}` = conv→GroupNorm→GELU→conv→GroupNorm→GLU→LayerScale); (b) decoder layers load `conv.weight` but the real keys are **`conv_tr.weight`** (transposed conv); (c) normalization order differs (demucs computes the spec from the RAW mix and normalizes the spec by mean over (1,2,3) + the time branch separately by mean over (1,2); the C# normalizes timeIn first then specs that). Reaching full parity = implement DConv + GroupNorm + conv_tr decoder keys + fix the norm order + verify the cross-transformer; a dedicated build. Spec front-end ✅. Oracle `tests/python-reference/demucs_reference/` (via `demucs` pkg); tests `DemucsSpecParityTests` (✅) + `DemucsFullParityTests` (scaffold-blocked). |
| **CSM** (Sesame 1B) | ✅ | **Full frame** bit-exact vs `transformers` CsmForConditionalGeneration (unsloth/csm-1b): backbone + c0 head c0 corr **1.000000** (maxAbs 8.8e-6, argmax exact) AND depth decoder all 31 acoustic codebooks **worstCorr 1.000000, argmax 31/31**; Mimi codec already ✅. End-to-end verified (CPU f32, 1B fits). | Backbone + c0 head + per-codebook audio/text embeddings + audio-frame context + projection + depth-decoder (`DebugFrameLogits`) all match. Depth decoder: position i embeds codebook-(i-1) of input via offset `(i-1)·2051`, pos 0 = backbone hidden, `codebooks_head[k-1]` predicts codebook k. Converted the transformers checkpoint to the C# sesame layout: `backbone_model.*`→`backbone.*`, `depth_decoder.model.*`→`decoder.*`, `lm_head`→`codebook0_head`, `embed_text_tokens`→`text_embeddings`, `inputs_embeds_projector`→`projection`, `depth_decoder.model.embed_tokens` [32·2051,2048] split into 32 `audio_embeddings.{i}`, `codebooks_head` [31,1024,2051] → 31 transposed `audio_head.{i}`. Oracle `tests/python-reference/csm_reference/`; test `CsmParityTests`. Needed transformers≥4.52 (env upgraded to 5.12). |
| **GPT-SoVITS HuBERT / CosyVoice sub-encoders** | ✅ | (validated above) | — |
| **PocketTTS** (continuous-latent) | ✅ | every neural component bit-exact vs the real `kyutai-labs/pocket-tts` (CPU f32): Mimi decode **corr 1.000000 / 3.3e-6**, FlowLM backbone **1.000000 / 1.1e-6**, flow_net **1.000000 / 6e-7**, end-to-end (fixed-noise) latents **1.000000** + audio **1.000000 / 2.2e-6** | Built from scratch to the reference: `PocketTtsMimiDecoder` (output_proj + depthwise ConvTrUpsample + 2-layer ProjectedTransformer [interleaved RoPE, sliding-window 250, LayerScale, exact-GELU] + moshi-fused SEANet), `PocketTtsStreamingTransformer` (shared backbone), `PocketTtsFlowNet` (SimpleMLPAdaLN: time RMSNorm unbiased/no-mean, LN eps 1e-6, modulate, SiLU), `PocketTtsFlowLm` (input_linear + bos + AR loop + lsd_decode). Used the ungated `pocket-tts-without-voice-cloning` weights. Tests `PocketTts{MimiDecode,FlowLm,Gen}ParityTests`; oracles `tests/python-reference/pockettts_reference/`. Remaining (non-numerical): wire the public `PocketTtsPipeline` to these + SentencePiece tokenizer + noise sampling. |

---

## IMAGE (diffusion) — see [`MODEL_STATUS_IMAGE.md`](MODEL_STATUS_IMAGE.md) for build detail
| Model | Parity | Notes |
|---|---|---|
| **SD 1.5 / SDXL / SD3.5 / Flux (Dev/Schnell/Krea) / Z-Image / Flux.2 Klein** | ✅ | Clean visual output verified end-to-end. Many plumbing bugs fixed (SD3 #31-35, Z-Image #25-30 in PHASE_3_DEVIATIONS). |
| **AuraFlow v0.3** | ✅ | Clean on-prompt horse+rider @1024 (`calcuis/aura` fp8, RTX 4090). Two conditioning bugs fixed: Pile-T5-XL attn scale 1.0 + correct `pile_t5xl_spiece.model` tokenizer (see §Bugs). Denoiser/VAE/scheduler correct as-built. |
| **Qwen-Image** (20B MMDiT, Q4_K GGUF) | ✅ | Clean photoreal on-prompt astronaut-on-horse @1024 on the 4090 (Q4_K_M GGUF + Qwen2.5-VL-7B fp8 TE + 16-ch VAE). **4 bugs fixed** (see §Bugs): final-layer scale/shift swap (the dominant grid bug), conditioning (template+drop-34 vs raw-512), GGUF rank-2 shape relabel, weight-cast OOM. Perf: GPU-residency block rewrite 30+min→~13min (per-GEMM Q4_K dequant is the remaining cost). Verified vs ComfyUI `comfy/ldm/qwen_image/model.py` component diff (rope ids / modulation order / attention / patchify all matched). |
| **Ideogram 4** (9.3B DiT) | 🔬 | ~1e-7 parity on 3060 after the GPU-residency rewrite (`dit_f32.ptx`). A100 timing pending. See [[ideogram4-gpu-residency]]. |
| **Anima** (Cosmos-Predict2 2B) | ✅ | Clean on-prompt "anime girl with blue hair in a garden" @512 on the 3060 (93.9s, precomputed Qwen3-0.6B embeds). |
| **Lumina-Image 2.0** (2B NextDiT) | ✅ | Clean on-prompt "serene mountain lake at sunset, golden light" @512 on the 4090 (53s, Gemma-2 embeds). **Required the DIFFUSERS-format weights+VAE** (`Alpha-VLLM/Lumina-Image-2.0` transformer/ + vae/) — the local AlphaVLLM original single-file (fused `attention.qkv`, `t_embedder`, `final_layer`) is NOT what the converter/`LoadWeights` expect (split `attn.to_q/k/v`, `time_caption_embed`, `norm_out`). TestPaths now defaults to the diffusers files. |
| **ERNIE-Image** (8B fp8) | ✅ | Sharp full-contrast astronaut-on-horse @512-cfg (std 60.9, grid 0.069; 3060, 7m26s). **4 bugs fixed:** (1) flat-black = BF16 BN stats read as F32→NaN (F32 cast in ctor); (2) general SDPA mask-drop ([B,1,1,Skv] element count matched no SDPA branch → dropped → image tokens attended padded text; emit [B,1,Sq,Skv] + CUDA B·Sq·Skv branch); (3) banding = tiled VAE decode (per-tile GroupNorm + bad overlap blend) → full non-tiled `Decode`; (4) **WASHOUT = `CudaBackend.GroupNormSilu` F32 path didn't cast BF16 affine weights** (Flux.2 VAE ships BF16 GroupNorm weight/bias; F32 activations → bytes reinterpreted as F32 → wrong affine in every VAE ResNet → 4-5× low contrast; the F16/BF16 paths cast, the F32 path was the gap — same dtype-mismatch class as the BN bug). Transformer was byte-perfect on a std-RATIO diff (the earlier corr diff MISSED the scale error — corr is scale-invariant). Also free TE weights right after encode (3060 OOM fix). TE (Ministral-3B, tap [-2]) + VAE config + tokenizer all confirmed correct (597 condEmb outlier is normal). |
| **Chroma** (8.9B, fp8) | ✅ | Clean on-prompt astronaut-on-horse @512 (painterly/ink style; `chroma_v1_512_nocfg_20260630_040338.bmp`, 93s). **Transformer NUMERICALLY VERIFIED** vs diffusers `ChromaTransformer2DModel` on identical fp8 weights: every component corr ≥0.999 (mod_table/approximator corr 1.00000, all 19 double + 38 single blocks ≥0.999, final velocity corr 0.99989). Root cause of the earlier full-frame noise: a **bad experimental checkpoint** — `Chroma1-HD-fp8mixed-final.safetensors` was symlinked to `/tmp/chroma_dl/do_not_use/…_exp.safetensors`; re-pointing to the proper top-level file → clean image. The transformer was always correct. |
| **Edit / image-conditioned variants** (Qwen-Image-Edit 20B · Boogu-Image-Edit · Flux.1 Kontext) | 🔧 | To download (fp8/GGUF) + e2e test. Qwen-Image-Edit Q5_K_M GGUF already local (SwarmUI); reuse the ✅ Qwen-Image transformer + edit conditioning path. Flux Kontext built (`flux1-dev-kontext_fp8_scaled`), visual e2e pending. See MODEL_STATUS_IMAGE §Edit variants. |
| **Chroma / ChromaRadiance / ZetaChroma / Hunyuan-Image / HiDream / Kandinsky-5 / OmniGen-2 / F-Lite / Krea-2 / Boogu-Image / Microsoft Lens / Lance-image / Flux.2 Dev (32B)** | 🔧 | Built end-to-end, numerics validation-pending. **Krea-2 (26GB bf16) + Boogu-Image (20GB) need fp8/GGUF downloads + e2e generation tests (only structural transformer tests exist). HunyuanImage 2.1: 35GB bf16 won't fit 24GB and the fp8/GGUF repacks use incompatible original-Tencent key naming — blocked pending an fp8-diffusers-naming quant or a GGUF/K-quant reader.** | Built end-to-end, numerics validation-pending (need Python-parity dumps + checkpoint download; several gated on VRAM). Status detail per model in [`MODEL_STATUS_IMAGE.md`](MODEL_STATUS_IMAGE.md). |

---

## VIDEO / WORLD MODELS — see [`PHASE_9_VIDEO.md`](PHASE_9_VIDEO.md), [`PHASE_10_INTERACTIVE.md`](PHASE_10_INTERACTIVE.md)
| Model | Parity | Notes |
|---|---|---|
| **Wan 2.2 TI2V-5B (T2V/I2V) / LTX-Video / LTX-2 / WanAnimate / WanS2V / WanVace / Lance-video / Kandinsky-5-video** | 🔧 | Built end-to-end, structural tests pass; numeric parity pending. See [[wan-build]], [[ltx-build]], [[ltx2-build]], [[lance-build]]. |
| **Matrix-Game 3.0 / Matrix-Game 2.0 / Oasis-500m / Hunyuan-GameCraft** | 🔧 | World models built (structural). Param audit + fixes done 2026-06-28 (see below). See [[matrix-game-build]], [[matrix-game-2-build]], [[oasis-build]], [[gamecraft-build]]. |
| **Cosmos-Predict1 V2W** | ❌ | Not started (FSQ tokenizer + AR transformer substrate). |

### World-model parameter audit (2026-06-28)
Full upstream-vs-port diff of every world model against its `config.json` / source. Fixes applied (still structural, real-weight numeric parity pending):

- **Hunyuan-GameCraft** — block counts corrected 19/38 → **20/40** (HYVideo-T/2; 19/38 were unused `__init__` fallbacks → would break weight load); Plücker layout corrected to `[cross(o,d), d]` (moment+direction) via opt-in `moment` flag on the shared `PluckerEmbedding` (MG3 keeps `[o,d]`, confirmed from upstream); CameraNet `scale` is a scalar `torch.ones(1)` (was per-channel) and PatchEmbed key is `camera_in.camera_in.proj.*` (was `patch_embed.proj.*`); CameraNet `forward` restructured to two factor-2 `compress_time` passes *between* the encode blocks (was a single deferred pool — wrong across the GroupNorm/ReLU nonlinearities); distilled preset de-aliased (8 steps / CFG 1.0 vs base 50 / 2.0); added `FlowShift`, `GuidanceEmbed`, `EmbeddedGuidanceScale`, `InferSteps`, `CfgScale`; action translation now spreads over chunk `duration` (not fps) along camera-local forward/right axes; added `CreateDefault` with upstream normalized intrinsics `[0.50505, 0.8979, 0.5, 0.5]`.
- **Matrix-Game 3.0** — `Base5B` default shape corrected to the shipped checkpoint **3072 / 24 heads / 30 layers / ffn 14336** (the 5120/40/40 `config.py` is stale/unused); added **`SigmaTheta = 0.8`** and implemented the per-head rope (`θ_h = 10000·(1+σ·linspace(-1,1,heads))`) as a backward-compatible `WanRope` path (σ=0 → byte-identical shared rope, verified Wan models unaffected); added **`TextLen = 512`** zero-pad/truncate of the umT5 context; `ActionBlocks` default is now blocks 0–14. `local_attn_size` intentionally NOT added — it is defined but **never used** upstream.
- **Matrix-Game 2.0** — verified all architectural params + 4 variants already match upstream; confirmed `warp_denoising_step` (shift-5 sigma warp) is implemented and now driven by config; added `WarpDenoisingStep`, `SinkSize`, `ContextNoise` fields for completeness (0/true in all shipped variants).
- **Oasis-500m** — config + pipeline constants verified correct (scaling 20/255, ddim 10, stabilization 15, noise_abs_max 20). One known follow-up: the **VAE MLP** should use exact (erf) GELU but uses `backend.Gelu` (tanh approx, ≤1.5e-4); needs a backend erf-GELU kernel (CPU+CUDA), deferred to avoid breaking GPU residency.

---

## 3D — see [`PHASE_11_THREED.md`](PHASE_11_THREED.md)
| Model | Parity | Components verified (maxAbs) | Notes |
|---|---|---|---|
| **TripoSR** | ✅ | DINO ViT-B/16 image tokens **8.5e-6** (corr 1.0) · Transformer1D scene_codes **3.4e-3** (corr 1.0) · NeRF probe density+color pass · 64³ density grid corr **1.0** — all CPU F32 vs the upstream `tsr` library on real `stabilityai/TripoSR` weights. | The scaffold was a wrong-architecture guess; **rewritten** to the real arch (see § Bugs). New faithful DINOv1 `ViTModel` encoder (`DinoViTEncoder`, PyTorch-exact bicubic pos-emb interpolation + exact-erf GELU), diffusers `Transformer1D` backbone (`TripoSrTransformer`: GroupNorm → proj_in → 16× BasicTransformerBlock [biasless QKV, GEGLU] → proj_out → residual → detokenize → ConvTranspose2d upsample), grid_sample-faithful `NeRFMLP` decoder. Oracle `tests/python-reference/triposr_reference/`; gated test `TripoSrParityTests` (`TRIPOSR_WEIGHTS` + `TRIPOSR_REF_IO`). E2E mesh follows by composition (all stages corr 1.0). |
| **Hunyuan3D-2** | 🔬 | **conditioner (DINOv2-giant SwiGLU) image_tokens maxAbs 7.3e-3, corr 1.0** vs HF `Dinov2Model` (CPU F32, real bundled weights); DiT + VAE rebuild scoped | Real ckpt is a **Flux-lineage MMDiT** (`model.double_blocks.{0..15}` + `model.single_blocks.{0..31}`, QK-RMSNorm, **no RoPE** — `pe=None`; `latent_in`/`cond_in`/`time_in`; `final_layer.adaLN_modulation`) conditioned by **DINOv2-giant** (1536/40L/24H, SwiGLU `mlp.weights_in/out`, native 518 so no pos-interp; bundled as `conditioner.main_image_encoder.*`) + VecSet **ShapeVAE** (`post_kl` 64→1024 → 16 `transformer.resblocks` [c_qkv packed, QK-RMSNorm on head_dim 64] → `geo_decoder` cross-attn [51-dim Fourier query, packed `c_kv`] → `output_proj`→1) + FlowMatchEulerDiscreteScheduler. The C# scaffold (simple AdaLN DiT w/ invented keys) is the **wrong architecture** → needs a from-scratch rebuild reusing the engine's Flux blocks; validate on CUDA (CPU forward impractically slow at this size — the giant conditioner alone is ~30 min/forward on the naive CPU backend). **Conditioner DONE**: added a SwiGLU FFN path + `Dinov2Preset.Giant` to the shared `Dinov2VisionEncoder`, verified corr 1.0 (oracle `tests/python-reference/hunyuan3d_reference/dump_dino_giant_reference.py`, test `Hunyuan3DDinoParityTests`). DiT/VAE rebuild spec in [`PHASE_11_THREED.md`](PHASE_11_THREED.md) § 2. Reference source `/tmp/hy3d/hy3dgen/shapegen`; ckpt `/tmp/hunyuan3d2`. |

---

## LLM / TEXT ENCODERS / VLMs / EMBEDDINGS — see [`MODEL_STATUS_LLM.md`](MODEL_STATUS_LLM.md), [`LLM_MODEL_COVERAGE.md`](LLM_MODEL_COVERAGE.md)
| Model | Parity | Notes |
|---|---|---|
| **Native LLM decoders** (`GenericTransformer`) | ✅ | Verified e2e on 3060: Llama1/2/3.x, Mistral, TinyLlama, SmolLM, Yi, Qwen2/2.5/3, Gemma-2/3, Phi-3.5/4-mini, StableLM-2, Granite-3, Command-R7B, OLMoE, Granite-MoE. Config-driven (preset + key-map). See [[llm-model-coverage-plan]]. |
| **VLMs** (Gemma-3, SmolVLM2, LLaVA, Qwen2.5-VL 3B+7B) | ✅ | Vision towers reference-validated **corr=1.0** (per-stage torch dumps) + e2e correct. `SiglipVlmEncoder` (SigLIP/CLIP) + `Qwen25VlEncoder`. Qwen2.5-VL-7B verified e2e (blue→Blue, red→Red); caught + fixed a CUDA cast int-overflow on large-vocab quantized heads. |
| **Llama-3.2-Vision-11B** (mllama) | ✅ | The splice-free VLM (vision → gated cross-attention layers). `MllamaVisionEncoder` **cos=1.000000 every stage** (`dump_mllama_vision_ref.py`: seq/blk0/postln/cat/embeds); `MllamaCrossAttentionLayer` slice-verified. e2e on 3060: red circle→"red", blue square→"a blue square with a white outline…". Ollama-converter gate quirk decoded (gates pre-tanh'd; `position_embd.gate`=1−tanh). See [[vlm-7b-mllama-build]]. |
| **Embeddings** (bge-small CLS, all-MiniLM mean) | ✅ | `BertEmbeddingModel`, **cosine=1.000000** vs HF transformers. Quant decode Q8/Q5/Q4/Q3 all >0.99 vs F32. |
| **T5 / UMT5 / Pile-T5 (AuraFlow) / BERT / SigLIP / Qwen3-VL vision tower** | 🔬 | Encoder diff tests (`T5EncoderDiff`, `BertModel`, `Siglip`, `Qwen3VlVisionTower`). Pile-T5 shares UMT5 **encoder architecture** but has its **own LLaMA-derived SentencePiece tokenizer** (vocab 32000, `pile_t5xl_spiece.model`) — NOT the t5_xxl/umt5 vocab; using the wrong one silently mistokenizes (see §Bugs, AuraFlow). Also attn scale = 1.0 (faithful T5), not 1/√d. See [[pile_t5_is_umt5]]. |
| **MoE / MLA build-defer** (Mixtral, Qwen3-MoE, DeepSeek-V2-Lite/V3, Kimi) | 🚧 | Built + slice/unit-tested; e2e pending >12 GB hardware (V2-Lite loads, OOMs at preload). |
| **DeepSeek-V3 / Kimi-K2 routing + q-LoRA** (Phase 8a) | 🔬 | **Slice-verified**: group-limited `noaux_tc` routing (sigmoid + e_score bias + group top-2-sum + routed_scaling) matches an independent HF port (`MoeTests.MoeFeedForward_GroupLimitedRouting_MatchesDeepSeekV3Reference`, maxdiff ≤1e-4); q-LoRA query block matches a host ref (`MlaTests.Mla_QLora_QueryBlock_MatchesReference`). Full e2e >12 GB. |
| **GPT-OSS attention sinks** (Phase 8c) | 🔬 | **Slice-verified**: per-head sink logit (CPU `AttentionReference` + `flash_attn_f32.cu`, PTX recompiled) matches an explicit `softmax([scores,sink])` reference + the two limits (`FlashAttentionTests.Flash_Sink_MatchesAugmentedSoftmax`). 20B+ e2e deferred. |
| **Llama-3.2-Vision (mllama) gated cross-attn** (Phase 8b) | 🔬 | **Slice-verified**: `MllamaCrossAttentionLayer` (Q=text, K/V=vision, bidirectional GQA, q/k RMSNorm, tanh gates, gated residual+FFN) matches a host reference + the tanh(0) no-op identity (`MllamaCrossAttentionTests`). Tiled vision encoder + decode integration + 11B-Q4 CPU e2e still build-defer. |
| **Remaining for FULL support** | 🚧 | Phase 6 (decoder-embeddings, rerankers, IQ-quants), Phase 7 (**Mamba/SSM, RWKV, hybrids, T5 seq2seq — new arch code**), Phase 8 e2e (mllama vision encoder + DeepSeek-V3/GPT-OSS/mllama full-model runs on >12 GB), Phase 9 (batch>1, spec-decode). See [LLM_MODEL_COVERAGE.md § Completion plan](LLM_MODEL_COVERAGE.md). |

---

## § Bugs found (and how they were caught)

Real bugs surfaced by the parity loop — kept here so the patterns repeat-catch across models.

| Model | Bug | How it was caught | Fix |
|---|---|---|---|
| Kyutai TTS | Cross-attention applied RoPE | Backbone diverged 2e-2 (bulk matched 1e-4); moshi `apply_rope` asserts `q.T==k.T` which fails for T≠S | moshi forbids rope in cross-attn — removed it. cb-0 backbone → 1.3e-4 |
| Kyutai TTS | RMSNorm eps 1e-8 (scaffold) vs **1e-5**; gating inner 8448 vs **5632** (⅔ trick); interleaved (not NeoX) RoPE; norm stored as `alpha` | Reading the moshi source while reconciling keys | Corrected all in `MoshiTransformer` |
| Kyutai TTS | Depformer weight layout wrong (per-(set,layer) q/k/v vs **packed** `in_proj_weight [33792,1024]` sliced by schedule) | Real safetensors key dump vs C# expectation | Rewrote `MoshiDepformer` with packed slicing |
| Kyutai TTS | `EmbedFrame` indexed `_emb[k][-1]` for the warmup zero-token (garbage) | Greedy generation parity: frame-0 cb-0 wrong (943 vs 759) | `if(code==_zeroToken\|\|code<0) continue` (moshi ScaledEmbedding `is_zero`) → cb-0 exact |
| Kyutai TTS | `MoshiDepformer.TextEmbed` didn't demux the multiplexed text token | Greedy parity (depformer test only used a small token 42, so out2 path untested) | Demux main/second via out1/out2 |
| Mimi (DSM) | SeaNet loader required `weight_g/weight_v`; DSM ships composed `conv.weight`; and `NResidualLayers` over-counted blocks per stage | `LoadWeights` KeyNotFound on `encoder.model.0.conv.conv.weight_g` then `model.2.block.1` | `LoadFusedConvWeight` accepts both formats; SEANet residual-count + decoder seqIdx fixed |
| GPT-SoVITS | s1 golden logits were **stale/wrong** (C# was right) | Independent reimpl + real upstream module both gave -6.16 | Fixed the test golden |
| GPT-SoVITS | HuBERT pos_conv weight-norm (dim=2) loader; GroupNorm needs rank-4; tanh→exact-erf GELU | HuBERT output ~zeros; diff vs PyTorch | `ComposePosConvWeightNorm`, rank-4 reshape, `ExactGelu` → 1.07e-5 |
| Qwen3-TTS | RoPE interleaved vs **split-half NeoX**; tokenizer dropped leading spaces (no GPT-2 byte-level) | bit-exact diff failed | `RopeStyle.SplitHalf` + `ByteLevelCodec.Encode` |
| Piper / all VITS | 7 VITS bugs (rel-pos attn zero-pad, folded EA `exp(-logs)`, resblock conv naming, …) | corr vs onnxruntime | fixed → 0.9998 |
| Spark-TTS (BiCodec) | decoder was a structural guess: 1024-D codebook (really **factorized 8-D**), mean-pooled d-vector, FiLM-lite, skipped the AdaLN PreNet | wav corr 0.04; per-layer dumps localized it | rewrote `BiCodecDecoder` to the real `detokenize` (PostNet is training-only) → wav corr 1.0 |
| Spark-TTS (BiCodec) | d-vector flattened the 32 FSQ codes **token-major**; reference transposes to `[128,32]` first (channel-major) | d_vector corr 0.22 while z_q was 1.0 | flatten channel-major → corr 1.0 |
| Spark-TTS (DAC wavegen) | reused `DacDecoder` assumed `k=2·stride` + per-C_out transpose weight-norm; Spark uses explicit kernels `[16,11,8,4]` + dim-0 (descript) weight-norm | ConvTranspose output length off by 1; then `WeightNormFusionT` C_out/C_in mismatch | added `DecoderKernelSizes` + `TransposeWeightNormDim0` opt-ins to `DacConfig` (defaults unchanged) |
| Spark-TTS (LM) | config token-ID bases were all placeholders (assumed semantic-before-global) | reconciling vs the real `added_tokens.json` | global=151665, semantic=155761; fixed config + a now-wrong test assertion |
| MeloTTS | test-harness fortran-order npy bug (transposed torch tensor saved F-contiguous, C# read C-order) misread the BERT input | m_p corr 0.907 while logs_p was 1.0 | `np.ascontiguousarray` on every dump → text-enc bit-exact |
| MeloTTS | `VitsDurationPredictor` did norm→ReLU; VITS DP is conv→**ReLU→norm** (Piper never hit it, uses SDP) | durations collapsed (62 vs 216 frames) | fixed order → DP corr 1.0 |
| F5-TTS | ConvNeXt text stem didn't mask the filler tail (text shorter than mel); a stale comment claimed it was unnecessary | text-stem output corr 0.62; per-stage dumps localized it | zero the filler tail before AND after every ConvNeXt block → corr 1.0 |
| F5-TTS | timestep sinusoid missing the ×1000 scale (`SinusPositionEmbedding(scale=1000)`); feeds AdaLN in every block | textemb/xinput corr 1.0 but block0 corr 0.87 (exploding over 22 blocks) | scale t by 1000 before the sinusoid → block0 corr 1.0 |
| F5-TTS | ConvNeXt stem used tanh GELU; it needs **exact erf** (`nn.GELU()`), while the DiT FFN needs **tanh** (`approximate="tanh"`) — they were swapped | velocity maxAbs 0.0015 (corr already 1.0) | erf in the stem, tanh in the FFN → maxAbs 2e-5 |
| F5-TTS | pipeline CFG was uncond-anchored (`uncond+cfg·(cond−uncond)`) and clamped the ref region every step | n/a (caught reading `CFM.sample`) | F5 is cond-anchored (`pred+cfg·(pred−null)`); ref region replaced only at the end → sample loop corr 1.0 |
| FishSpeech | checkpoint is fused-key Llama (`attention.wqkv`, `feed_forward.w1/w2/w3`, `attention_norm`) wired to `Qwen2Model` (split keys) | `LoadWeights` would KeyNotFound | key adapter: slice wqkv→q/k/v, rename w1/w3/w2→gate/up/down + norms |
| FishSpeech | fish Llama uses interleaved (complex `view_as_complex`) RoPE; `Qwen2Model` was split-half | slow logits corr 0.84 | added `Qwen2Config.Rope`; set `Interleaved` for fish → corr 1.0 |
| FishSpeech | C# scaled the summed codebook embedding by 1/√(N+1); the shipped checkpoint loads `scale_codebook_embeddings=False` | (folded into the corr-1.0 fix) | config-gated the scale off |
| Dia | published checkpoint is nari-native `DenseGeneral` (`q_proj` `[in,h,k]`, `o_proj` `[h,k,out]`, `wi_fused` `[in,2,ffn]`), prefix `encoder.*` not `model.encoder.*` | encoder corr 0.35 | `DiaWeights.Adapt`: flatten + transpose to `[out,in]`, rename `wi_fused`/`wo` |
| Dia | C# used GPT-J interleaved RoPE + `1/√head_dim` attn scale; Dia uses split-half RoPE + **scale 1.0** | encoder corr 0.35 | `RopeSplitHalfInPlace` + `scale=1.0` → encoder corr 1.0 |
| Dia (decoder) | `DiaDecoder` never called `StreamingKvCache.AdvanceLength()`; `Append` writes at the shared CurrentLength but does NOT advance (caller must, once per step after all layers) | decoder step 0 corr 1.0 but step 1+ garbage (every step overwrote position 0) → corr 0.34 | `cache.AdvanceLength(1)` per step → all-step corr 1.0 |
| FishSpeech (fast) | `GenerateFrame` fed the fast depth-LM the POST-norm slow hidden; `norm_fastlayer_input=False` → it needs the PRE-norm hidden (slow head still uses post-norm) | (caught reading `BaseTransformer.forward`) | `ForwardEmbeds(applyFinalNorm=false)` + apply the slow norm only for the head |
| MusicGen (T5) | T5 attention scaled by `1/sqrt(head_dim)`; **all T5 (incl. MusicGen's google/t5-base) use scale=1.0** ("Mesh-TF: avoid scaling before softmax") — caused block-0 maxAbs 294, full-output corr 0.40 | Per-component T5 dump: block-0 corr 0.96 with exploding magnitude | `T5TextEncoderConfig.AttentionScale` (null → old 1/sqrt for the unchanged Flux/SD3-XXL path; `1.0f` for `T5Base`) → T5 hidden corr 1.0 |
| MusicGen (T5) | `T5Block` hardcoded v1.1 GEGLU (`wi_0`/`wi_1`, GELU); MusicGen's T5 v1.0 has a single `wi` + ReLU `DenseReluDense` | `LoadWeights` KeyNotFound on `...DenseReluDense.wi_0.weight` | Config-gated non-gated FFN (`GatedFeedForward`/`UseReluActivation`, already on `T5Base`) wired into the block |
| MusicGen (decoder) | sinusoidal positional embedding put **sin in the first half, cos in the second**; HF `MusicgenSinusoidalPositionalEmbedding` is `cat([cos, sin])` (cos first) | Reading the HF source while building the decoder-logits dump | swapped to cos-then-sin → decoder logits corr 0.999999 |
| EnCodec (32/16 kHz) | SEANet convs used **zero (constant) padding**; the published EnCodec checkpoints use **reflect** padding (HF `EncodecConfig.pad_mode` default), and the C# always padded **causally** (left-only) instead of the non-causal symmetric split | Per-stage decode dump: latent + weights bit-exact but stage-0 (initial conv) corr 0.99; the interior matched while the first ~3 samples were shifted | `EnCodecConvPad.PaddedConv` (causal/symmetric split + zero/reflect fill); `PadMode` default → `"reflect"` (Mimi keeps explicit `"constant"`) → decode bit-exact (1.4e-6) |
| EnCodec (decoder) | `MusicGenCheckpointConverter` transpose-conv detection keyed on `weight_g` shape `[1,C,1]`; HF/AudioCraft weight-norm `ConvTranspose1d` with **dim=0** → `weight_g [C_in,1,1]` (indistinguishable from Conv1d) so convtr layers were emitted as `conv.conv` → KeyNotFound `decoder.model.3.convtr.convtr.weight`. Also `WeightNormFusionT` fused per-**out**-channel; dim=0 is per-**in**-channel | Original load crash; then convtr fusion isolation (corr 1.0 only after the axis fix) | detect transpose decoder convs **structurally** (top-level decoder convs except the first/last Sequential index); `WeightNormFusionT` fuses along dim=0 (per-in-channel) |
| HeartMuLa (LM) | Decoder config was a structural guess: `HiddenSize=1024, 16 heads, 8 kv, FFN 4096, 3 layers`. Real torchtune `llama-300M` is **3072-dim** (same as backbone), 3 layers, **8 q / 4 kv** heads (head_dim 384), FFN 8192; the backbone→decoder `projection` is therefore 3072→3072 (not 3072→1024) | Real safetensors header: `decoder.layers.0.attn.q_proj [3072,3072]`, `k/v_proj [1536,3072]`, `projection.weight [3072,3072]` vs the C# config | Fixed `HeartMulaConfig` decoder dims |
| HeartMuLa (LM) | C# expected per-codebook `audio_embeddings.{i}.weight` + `audio_head.{i}.weight` and HF-Llama layer names; the real checkpoint is **torchtune**: one fused `audio_embeddings.weight [8·8197,3072]`, one fused `audio_head [7,3072,8197]` Parameter, and `attn.{q,k,v}_proj`/`attn.output_proj`/`mlp.w1,w3,w2`/`sa_norm.scale`/`mlp_norm.scale`/`norm.scale` | `LoadWeights` would KeyNotFound on the first layer | new `HeartMulaCheckpointConverter`: key remap + split the fused embedding into 8 row-blocks (`code + cb·vocab`) + split/**transpose** `audio_head[i]` from `[in,out]`→`[out,in]` |
| HeartMuLa (LM) | RoPE: both transformers are torchtune `Llama3ScaledRoPE` (interleaved/`view_as_complex`, **scale_factor=32**, low 1 / high 4 / old_ctx 8192) — the C# config had plain θ=500k split-half RoPE, no scaling | Reading the upstream `modeling_heartmula.py` `FLAVORS`; the engine `RopeScaling.Llama3` matches `apply_scaling` line-for-line | `RopeScaling.Llama3` (factor 32) + `RopeStyle.Interleaved` on both `Qwen2Config`s; backbone+heads then argmax-exact vs the oracle |
| HeartMuLa (LM) | `CsmModel` depth-decoder path: applied `projection` only to the backbone hidden + truncated the audio embeds to `dh` dims + stopped on `== 8192`. Upstream `generate_frame` projects the **whole** `[last_h, embed(c0), …]` sequence (full 3072 dim, no truncation) and stops on **`>= 8193`** | (folded into the corr-0.9998 decoder fix; caught reading `generate_frame`) | rewrote `BuildDecoderSequence` (full-dim audio embeds, project whole seq) + EOS `>= AudioEosToken` |
| YuE (Stage-1) | `YueConfig.V1.Stage1` declared **MHA** (`NumKeyValueHeads=32`) and **vocab 100000**; the real `config.json` is **GQA (`num_key_value_heads=4`)** and **vocab 83968** | Real safetensors header: `k/v_proj.weight [512,4096]` (= 4·128) and `embed_tokens [83968,4096]` vs the C# config; teacher-forced logits parity would not even run with the wrong KV-head / vocab dims | fixed the two dims → LM logits corr 1.0, argmax 8/8 |
| YuE (tokenizer) | `YueTokenizer` resolved the markers as `[SOA]`/`[EOA]`/`[stage_1]` via `EncodeToIds` — but they are SP **CONTROL symbols** spelled `<SOA>`/`<EOA>`/`<stage_1>`; `EncodeToIds("<SOA>")` byte-falls-back to 4 ids and Microsoft.ML.Tokenizers' `SpecialTokens` is empty → `ResolveSpecial` **threw at construction** | Real `tokenizer.model` probe: `Decode([32001]) == "<SOA>"` but `EncodeToIds` ≠ single id | pin the fixed mm_tokenizer_v0.2 ids (32001/32002/32013) + confirm each via a `Decode(id)` round-trip |
| YuE (XCodec) | The C# `XCodec` (DAC-style: factorized RVQ `quantizer.quantizers.*.in_proj/out_proj`, 8 codebooks, no `fc_post2`) was **wrong-architecture** for the real `xcodec_mini_infer` codec (SoundStream: EMA-VQ `quantizer.vq.layers.{0..11}._codebook.embed [1024,1024]`, 12 codebooks, `dac2` `decoder_2`, mandatory `fc_post2` 1024→256) | `XCodec.LoadWeights` threw `KeyNotFound: quantizer.quantizers.0.in_proj.weight` | **FIXED** — rewrote `XCodec` to the real SoundStream arch (`XCodecEmaResidualVectorQuantizer` + `fc_post2` + dac2 decoder) + `YueCheckpointConverter.LoadXCodec` for `codec_model`; decode now **corr 1.000000** vs upstream |
| HeartCodec (estimator) | `HeartCodecEstimator.AdaLnSingleFlow` fed a **rank-2 `[b,dim]`** tensor to `WhisperOps.ProjectLinear` (which silently heap-corrupts on rank-2 — needs `[b,1,dim]`), the SiLU'd timestep embedding | SIGSEGV inside the estimator on first real-weight run, bisected with stderr step-prints (adaln crashed right after proj_in) | keep the SiLU output rank-3 `[b,1,dim]` (Silu from the still-rank-3 `embedded`) → estimator runs |
| HeartCodec (estimator) | The transformer-block loop did `s.Dispose(); s = ns` but **`Block.Forward` mutates its input in place and returns the SAME tensor** → it freed the live buffer and the next block read freed memory | SIGABRT (heap corruption detected on a later free); bisect showed "block0 OK" then abort before "all blocks OK", and it reproduced at b=1 (ruling out the CFG batch) | `s = _blocks[i].Forward(...)` with NO dispose between iterations (same for the stage-2 loop); full decode then runs finite + estimator corr 1.0 |
| Engine-wide | CPU SDPA rank-2 causal mask mis-indexed for heads > 0 (OOB → flaky NaN) | Found while validating Boogu-Image's `LlamaStyleEncoder` | `AttentionKernels` branches on mask rank |
| Engine-wide | `WhisperOps.ProjectLinear`/`BatchedMatMul` heap-corrupt on rank-2 input | ECAPA/Zonos stat-pooling crashes | Always pass `[1,seqLen,inDim]`. See [[projectlinear-needs-rank3]] |
| Engine-wide | CPU MatMul/Linear heap-corrupt on bf16/f16 weights | Audio loaders | Cast to F32 first (`RequireF32`). See [[cpu-kernels-f32-only]] |
| AuraFlow v0.3 | Pile-T5-XL encoder `AttentionScale` unset → `T5Block` fell back to `1/√head_dim` (0.125); faithful T5/UMT5/Pile-T5 use **scale 1.0** across all 24 layers → corrupted conditioning | Official-source review (diffusers/ComfyUI T5 stacks); sibling `T5Base` preset already set 1.0 | Added `AttentionScale = 1.0f` to `T5TextEncoderConfig.PileT5Xl` |
| AuraFlow v0.3 | Test fed `t5_xxl_spiece.model` to the Pile-T5-XL encoder, but Pile-T5 uses its **own LLaMA-derived SentencePiece** (vocab 32000, different token ids) → clean-but-off-prompt image (rendered, just not the prompt) | @1024 produced a flawless photoreal image with **wrong content**; token-id diff showed astronaut/horse → `[385,29132,…]` (pile) vs `[46,30059,…]` (xxl) | `TestPaths.PileT5XlSpiece` now defaults to `pile_t5xl_spiece.model`; md5 `eeec4125…` byte-matches EleutherAI/pile-t5-xl `spiece.model`. Combined with the scale fix → on-prompt horse+rider, promoted ✅ |
| Qwen-Image | **Final layer (norm_out / AdaLayerNormContinuous) had scale/shift SWAPPED** — `ApplyFinalLayer` read `shift=firstHalf, scale=secondHalf` (the per-block AdaLayerNormZero `[shift,scale]` order), but the continuous final norm is `scale, shift = chunk(emb,2)` (**scale first**). Velocity collapsed (std 0.57), latent never denoised → VAE rendered noise as a woven grid | Diffed our transformer against ComfyUI `comfy/ldm/qwen_image/model.py` `LastLayer` component-by-component (rope ids / modulation / attention all matched; only the final layer differed). `[DIAG]` logs: noisePred std 0.57, finalLatent std 1.6 (≈ unchanged from noise) | Swapped to `scale=firstHalf, shift=secondHalf` → noisePred std 1.29, finalLatent 0.65, clean photoreal astronaut-on-horse. See [[adaln-continuous-scale-shift-order]] — check this order for EVERY DiT model's norm_out |
| Qwen-Image | Conditioning fed the RAW prompt **padded to 512** (Qwen3Tokenizer), no encode-template, no prefix-drop — diffusers `_get_qwen_prompt_embeds` needs the ChatML template + real length + drop 34 | condHidden seqLen was 512 (mostly padding); diffusers/ComfyUI use real length | `Qwen2Tokenizer.EncodeChat(prompt, qwenImageSystem, addGenerationPrompt:true)` + `promptDropIndex:34` → seqLen 512→12 (reused the `HunyuanImageQwenTextEncoder` pattern) |
| Qwen-Image (GGUF) | GGUF rank-2 weight shapes are ggml `[in,out]`; the image GGUF path never relabeled to PyTorch `[out,in]` (the LLM path does) → first Linear got M=0 → zero-grid kernel launch `CUDA_ERROR_INVALID_VALUE` | First transformer forward crashed in `LaunchBiasAddImpl` | shared `GgufModelLoader.RelabelRank2ToPyTorchOrder` applied before the converter. See [[gguf-diffusion-load-gotchas]] |
| Engine-wide (mmap) | A loader that returns only `GetAllTensors()` dropped the `SafeTensorsLoader`; a GC during the big GGUF convert finalized `MmapHandle` → unmapped the fp8 TE mid-load → AccessViolation in `ApplyFp8ScaledDequant` | Crash loading the fp8 text encoder right after the 1933-tensor GGUF convert | `Tensor.SetKeepAlive` + `SafeTensorsLoader`/`GgufLoader` root the handle. See [[mmap-tensor-keepalive]] |
| TripoSR (whole model) | The entire scaffold was a wrong-architecture guess: invented keys (`triplane_pos`, `image_proj`, `out_proj`, `self_attn.q.bias`, `mlp.fc1`, `fc_in`/`fc_hidden`), no triplane upsampler, no-affine LayerNorm, GELU-MLP FFN, DINOv2 key layout, [0,1]+clamp sampling | First real-key inspection: real ckpt has `tokenizer.embeddings [3,1024,32,32]`, `backbone.{norm,proj_in,proj_out,transformer_blocks.*}` (diffusers `Transformer1D`), `post_processor.upsample` (ConvTranspose2d), `decoder.layers.{0,2,…,18}`, `image_tokenizer.model.*` (HF `ViTModel`) | Rewrote to the real arch: `DinoViTEncoder` (HF ViT keys `layernorm_before/after`/`intermediate.dense`/`output.dense`), `Transformer1D` (GroupNorm32 → proj_in → 16× BasicTransformerBlock [biasless QKV, affine LN eps 1e-5, GEGLU] → proj_out → residual → detokenize → upsample), `NeRFMLP` (`layers.{2i}`). → all components corr 1.0 |
| TripoSR (DINO) | DINO is **DINOv1 `facebook/dino-vitb16`** (patch 16, native 224) at a 512px cond image → needs **bicubic position-embedding interpolation** (14²→32²) and **exact-erf GELU**; `backend.Gelu` is the tanh approx (drifts over 12 layers) | image_tokens diverge without it | PyTorch-exact `interpolate_pos_encoding` (cubic-convolution A=−0.75, align_corners=False, `src=scale·(dst+0.5)−0.5`, edge-clamp) + `VitOps.ExactGelu` → maxAbs 8.5e-6 |
| TripoSR (NeRF sampling) | Triplane sampling used [0,1]+edge-clamp bilinear (align_corners=True); `query_triplane` uses `F.grid_sample(align_corners=False, padding_mode="zeros")` with coords `p/radius` in [−1,1]; density activation is `exp(density−1)` (bias −1.0) at iso **25**, not raw at iso 10; plane stack is (x,y)/(x,z)/(y,z) with first coord→W | (caught reconciling `TriplaneNeRFRenderer.query_triplane` + `extract_mesh`) | `GridSampler.GridSamplePlane` (align_corners=False, zeros pad) + exp activation + threshold 25 → probe + 64³ grid corr 1.0 |
| ERNIE-Image | VAE `bn.running_mean/var` ship as **BF16**; `ApplyBnUnnormalize` read them as raw `(float*)DataPointer` → reinterpreted the 256-byte BF16 buffer as F32 (2-byte elems read as 4-byte: garbage for the first half, OOB past the end) → NaN in ~25/128 channels → `latent*std+mean` NaN → **flat-black image** | `[DIAG]` mean/std/NaN per stage: condEmb/noisePred/finalLatent all clean (0 NaN), `latent_postBN` had **25600 NaN** | Cast BN stats to F32 in the `ErnieImagePipeline` ctor (was relying on the caller; the pipeline now self-protects). **General lesson: any `(float*)tensor.DataPointer` read must ensure DType==F32** (same class as [[cpu-kernels-f32-only]]). |

---

## Conventions / where things live
- **Reference dumps + scratch:** per-model under the session scratchpad; the moshi/audio refs live in
  `scratchpad/kyutai/`. Reference `.py` scripts are kept beside their dumps.
- **Parity tests:** `tests/HartsyInference.Audio.Tests/*ParityTests.cs` (gated on env vars pointing at the real
  checkpoint + the dumped reference). Image/video parity uses the per-model `*DebugDump` + diff harness.
- **Build:** audio with `-m:1`; tests crash under xunit parallel → run sequentially
  (`-- xUnit.ParallelizeTestCollections=false`). Reuse a model cache via `HARTSYINFERENCE_MODEL_CACHE`.
- **Never** commit/push — the user does all git manually.
