# Research

Produce implementation-relevant facts: exact constants, shapes, byte/key layouts, reference differences, provenance and a validation plan. Use primary reference code and pin revision/date; distinguish source snapshots from current build/verification status.

Search existing research, modality status, PARITY_VERIFICATION and relevant TROUBLESHOOTING entries first. Do not duplicate facts recoverable from our code or create empty outlines. Use the deep-research skill only when its own trigger conditions are met.

A useful note names the specific reference behavior (e.g. timestep cosine/sine order and denominator), why it matters, and how to compare saved intermediate tensors. Share saved inputs, not just RNG seeds. Clearly mark unresolved questions; do not turn an assumption into a claim.

Keep only applicable sections: key numbers/layouts, non-obvious algorithm, references, validation and open questions. Label dates as source snapshots; research completion does not establish engine verification.
